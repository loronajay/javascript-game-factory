# Changelog

## Farm pets: stat progression (2026-09-25)

**Speed and Strength now grow instead of being locked at adoption.** A pet is born with a base and a hidden per-stat growth rate from a weighted **potential** roll (Steady / Gifted / Exceptional / Prodigy — a Prodigy is ~1% on a classic look and 12% on a super rare), and earns stat points every living farm day: fastest in youth, slower as an adult, none as an elder, which keeps its peak. How much of a day counts is a care multiplier — fed, happy, trusting (hidden affection) and **treated the way its traits want** (hidden rapport, built by trait-matched petting/carrying/play/feeding with a fading daily allowance, lost by e.g. grabbing an Independent pet or feeding a Light Eater that is not hungry). Pure layer `js/farm-pet-growth.mts`; the checkpoint integrates long absences day by day. The Pets panel shows stars + grade, life stage, a trend and `+N` earned per stat — never affection or rapport. Old pets migrate with a stable seeded roll and credit for the days they already lived. The server stopped pinning `stats` outright: `services/farm-pet-growth-policy.mts` mirrors the maths (parity test against the client module), pins the rolled grade/base/rates, bounds earned growth by age, and recomputes `stats` itself. Design detail in `FARM_PETS_AND_CARE_PLAN.md` → Stat progression.

## Tickets: Creature Battler, Shark Hall, Mini Hoops and Puck'd Up (2026-09-24)

**Four more cabinets pay out through `POST /games/:slug/results`**, each as a normalizer + pure formula in `services/<slug>-ticket-rewards.mts`, a pure builder in the cabinet, and a mint-on-start/spend-on-end reporter. Every formula earns completion by time (1 per 10 s; Mini Hoops 1 per 15 s of round clock) and every one holds the whole payout to a per-second ceiling (1 per 7 s; Mini Hoops 1 per 5 s), so a forged win costs real wall-clock time under the fence. Creature Battler files training and online battles (completion ≤25/30, win 10/15; draws pay completion; a disconnect win is not filed). Shark Hall files CPU racks (completion ≤25, win 5/10/15 by Casual/Club/Sharp) and online races (completion ≤25/75/125 by race length, win 10/25/40); hotseat is not filed. Mini Hoops files the timed round only — solo and the online duel (round clock + 1 per 3 baskets, +2/+4 online win); hotseat, HORSE, tic-tac-toe, the Lab and forfeits are not filed. Puck'd Up files quick matches, Arcade Circuit matches and online matches to 7 (completion ≤20, win 4 for Jin up to 15 for the Ace, 12 online); online forfeits are not filed. Each server test mirrors the cabinet's own catalog (rival ladder, difficulties, race lengths, hoop modes, round lengths) so a new entry fails CI until it is priced.

**Three traps found on the way.** (1) Shark Hall's page never loaded `js/platform-config.mjs`, so its **saved tables were silently not reaching the account in production either** — both are fixed by the one script tag, and a test now reads it. (2) Shark Hall's network server ends a match for whoever stayed when a seat walks, and the cabinet hears that as an ordinary `win`; the builder declines any table with a disconnected seat. (3) Creature Battler's `window.state` was always `undefined` (a classic script's `const` is a lexical global), so its activity posts had been going out with empty teams; the end hook now passes the battle facts explicitly, which fixes both.

## Tickets: Mini-Tactics, Bird Duty and Illuminauts (2026-09-24)

**Three more cabinets pay out through `POST /games/:slug/results`.** Each is a normalizer + pure formula in `services/<slug>-ticket-rewards.mts`, a pure `ticket-result` builder in the cabinet, and two call sites (mint when play starts, report when it ends). Mini-Tactics files CPU matches (Easy/Normal/Hard) and online 2–4P matches that end by elimination: 1 completion per 10 s capped by board (60 on 10×10, 90 on 13×13; online 120), and a win bonus of 5/15/25 (CPU) or 30 (online). Hot seat, the tutorial, desync/disconnect, concede-decided matches, a seat that resigned and watched the rest, and CPU matches with custom squads (the player picks the CPU's squad) are not filed. Bird Duty files solo runs (score-driven, never faster than 1 ticket per 7 s, cap 10) and online matches (1 per 10 s up to 15, plus 5 for winning a duel or 8 for a 3–4P table); hot seat is not filed. Illuminauts files solo Sprint/Sweep clears (1 per 10 s capped at 15/35, plus 5/10 under par) and finished online races (up to 20, plus 10 to the winner); playtests and abandoned races are not filed, and a test holds the server's par table equal to `scripts/maps.js`.

**Two lessons from driving them headlessly against the real normalizers.** (1) *A flat win bonus is forgeable too.* A faked 4-second Mini-Tactics "win" against Hard would have paid 25, and the time fence does not stop a claim that short, so the win bonus now also accrues at 1 per 12 s (an honest win of five minutes or more gets all of it). (2) *Blind play scores more than you'd guess.* Ten Bird Duty drops fired blind scored 35, so solo pay is capped by time as well as score, which keeps spraying near 420/h while a careful run earns about 500/h. The same pass found that Mini-Tactics and Illuminauts never loaded `js/platform-config.mjs`, so in production their API URL would have resolved to empty and nothing would have been filed. Both pages now load it. Bird Duty and Illuminauts import the platform client lazily (`scripts/ticket-reporter.js`), as their identity modules do, so they still boot when served standalone.

## Tickets: Battleshits and Sumorai (2026-09-24)

**Cabinets without an achievement run now have their own ticket path.** Lovers Lost settles inside its achievement transaction; every other cabinet now goes through `POST /games/:slug/results`, registered in `services/game-result-catalog.mts` (a normalizer per game) beside `ticket-reward-catalog.mts` (a pure formula per game). `db/game-results.mts` does one atomic settlement: dedupe by `(player, game, resultId)`, evaluate, credit the ledger under `game-result:<slug>:<resultId>`, store the verdict in `game_ticket_results` (migration 051). A retry replays the stored verdict. A payload naming an amount is refused, not stripped. The browser side is one reporter, `js/platform/api/game-results-api.mts`, which also publishes the new balance. Each cabinet adds a pure `scripts/ticket-result.js` and two call sites.

**A time-budget fence bounds forged results.** A new result may not claim more play time than has passed since the player's previous result on this path (any cabinet; 10% + 10 s slack). Honest play always passes; back-to-back scripted posts earn nothing past the first, and the zero verdict is stored so the id cannot be retried later.

**Completion is earned by time, not by finishing.** Playing it headlessly showed that standing still against Sumorai's Hard CPU loses a best of three in ~13 s. With a flat completion payout that was ~900 tickets/hour for doing nothing. Completion now accrues per second of match (Sumorai 1 per 15 s, Battleshits 1 per 9 s) up to its cap, and win bonuses stay flat because winning is the skill. Formulas: Battleshits CPU 20 completion + 5/15/25 win (Easy/Medium/Hard), online 25 + 20, +5 for ≥50% accuracy on a win, cap 50, forfeit wins 0 and not filed. Sumorai CPU 4/6 completion (Bo3/Bo5) + 1/3/5 or 2/5/8 win + 2 flawless, online 10 + 10 + 5 ranked win, cap 25; local 2P and online forfeits are not filed. Both are plausibility gates, not proof. Verified in real browsers against the real route: a 4.4-min Easy Battleshits win settled 25 tickets, and a thrown 13 s Sumorai loss settled 0.

## The Farm: Exotic Super-Rare Pets (2026-09-23)

**Rare coats are designed palettes rather than whole-model darkening filters.** Selective three-color atlas recoloring now protects eye whites, pupils, outlines, mouths, and pale facial details while retaining the source model's shading. Every species also has a new exotic Super Rare look with a subtle pearlescent finish: Cosmic, Prism, Celestial, Opaline, Nebula, Crystal, Eclipse, Voidfin, Biolume, and Starborn.

**The adoption table is now 69/24/6/1.** Rare pets receive +8% generated Speed and Strength; Super Rare pets receive +15%, capped at 100. The applied bonus is persisted through the API and normalized as a one-time migration for existing rare profiles, so reloads never compound it. The Pets panel explicitly identifies Super Rare palettes.

## The Farm: Pet Outcomes and Memorials (2026-09-23)

**Phase 5 closes the care loop.** The Pets panel now escalates readable care warnings without exposing affection, severely unhappy pets refuse food and handling, and H whistles every sufficiently bonded, happy pet toward the player. Low affection and happiness receive deterministic once-per-farm-day outcome rolls, with permanent runaway checked before the rarer neglect-death rule, so reloading cannot reroll fate.

**Death is an atomic persisted transition.** A pet that exhausts its starvation grace or reaches its natural lifespan leaves the live pet list and creates a unique procedural memorial stone plus a durable history record containing its name, species, final visible stats, traits, lifespan, outcome, and future accomplishment slots. Memorials can be moved and rotated, cannot be copied or added from the catalog, and require an explicit permanent-removal warning; removing the stone keeps the history. The browser and API trust boundaries normalize and preserve both records.

## The Farm: Every Pet Has a Home and a Look (2026-09-23)

**The visual loose ends in Phases 3 and 4 are closed for all ten species.** The build catalog now includes a distinct procedural home for every pet: Doghouse, Duck Coop, Treetop Den, Burrow Lodge, Mud-Wallow Shelter, Rhino Shade, Roosting Box, Reef Grotto, Darkwater Cave, and Jellyfish Lagoon. Each is ordinary placeable farm decor, identifies the species it supports, and records a visible entrance sized against that species' actual height and personal-space radius. The nine new models live in their own dwelling module instead of expanding the already-large general props file.

**Palette identity is visible now.** Every animal initially shipped with a species-specific Classic, uncommon, and rare palette weighted 70/24/6; the later Super Rare pass above supersedes that distribution and renderer. Adoption assigns a palette through the injectable random pipeline, valid ids survive client and API normalization, and non-classic palette names appear in the Pets panel. Catalog-wide tests enforce ten covered species, ten unique dwelling models, usable entrances, complete 100-point palette tables, persistence, and substantial procedural geometry.

## The Farm: Pet Age and Growth (2026-09-23)

**Phase 4 now has a live lifecycle instead of static profile fields.** Pet age and current size advance through the same persisted farm-time checkpoint as hunger and happiness, including naps and elapsed time away. Partial days survive the API trust boundary, Fast Grower reaches the individual adult cap sooner, and natural age stops exactly at each species maximum pending the atomic death work in Phase 5. The individual size multiplier now drives the rendered animal, collision/personal space, pond fit, carry/drop spacing, and surface-based interaction reach together; the Pets panel shows age to one decimal and current size without exposing affection. Palette rarity remains deliberately gated on real palette assets, and species dwelling models remain a separate Phase 3 asset pass rather than a Phase 4 dependency.

## The Farm: Pet Happiness and Real Seed Portraits (2026-09-23)

**Phase 3 care now changes the pet instead of merely decorating the field.** A species-appropriate dwelling grants its pet a persisted one-time +10 affection award, then offsets five points of the daily eight-point happiness drain and adds one affection per day. Each distinct compatible placed toy offsets two more happiness points and adds half an affection point per day. Cuddly and Independent now change petting and carrying responses; low-trust Independent pets visibly refuse, severely distressed pets warn and snap, and a pet with limited carrying patience warns before wriggling free. Y is a separate Play action that requires a compatible placed toy and improves happiness and affection. The API trust boundary preserves the one-time milestone so removing and replacing a dwelling cannot farm the award.

**Seed cards show the crop, not initials.** The inventory lazily renders each catalog crop's actual fully-grown GLB into a cached portrait and uses that image on the seed card. The old two-letter glyph is gone.

## The Farm: Feeding and the Shared Farm Clock (2026-09-23)

**Pet care now moves with the farm instead of the frame loop.** Every profile checkpoints hunger and its zero-hunger grace against the existing persisted farm-minute clock, so normal play, naps, leaving, and returning all advance the same timeline while clock rollback cannot replay elapsed time. Hunger drains 25 points per farm day before data-driven Big Appetite (1.5×) and Light Eater (0.65×) modifiers; time below 40 lowers hidden affection, and a full farm day at zero records that starvation death is due for the later atomic death/tombstone transition.

**G gives one matching species serving.** Feeding first checkpoints elapsed needs, consumes exactly one food only when the pet is below 100, restores 35 hunger up to the cap, and clears the starvation timer. Full pets and missing-food attempts consume nothing. The world prompt and Pets panel now show Full, Content, Hungry, or Starving without revealing affection, and the plan records that pets share farm time, away notifications are unnecessary, runaways are permanent, tombstone deletion is permanent, and gender remains female/male. Focused browser and API tests cover elapsed time, trait rates, threshold crossings, rollback, caps, repeated feeds, missing food, and trust-boundary persistence.

## The Farm: Every Pet Is an Individual (2026-09-23)

**The corgi no longer owns the pet system.** All ten species now receive the same complete persisted profile at adoption—gender, age, individual current/adult size and growth, hidden affection, hunger, happiness, speed, strength, 1–3 compatible traits, and palette identity. Species rows weight speed and strength so a rhino is meaningfully stronger than a duck and a bat skews faster than a hippo, without removing individual variation. Old profile-less pets migrate through stable species-and-instance-id entropy, so they gain the same individuality and traits once without rerolling on reload. Each species also has its own named food, provisional exotic-weighted price, maximum life, and data-only dwelling; the inventory preserves and shows every food stack while only the starter dog begins with 20 servings.

**Swimmers can come up for a cuddle.** Sharks, anglerfish, and jellyfish now use the same C pick-up path as every other pet, can ride safely outside their pond while carried, and can only be put down where their whole radius fits inside a water region. Ground/air placement inside buildings remains intact. All 122 farm tests, focused API trust-boundary tests, both strict TypeScript checks, and browser/API generated-file verification are green.

## The Farm: First-Farm Dog Onboarding (2026-09-23)

**A new farm now begins with a relationship, not an empty catalog.** A persisted onboarding marker distinguishes a genuinely new farm from every legacy or deliberately empty farm. Owners see the introduction once, must give their starter corgi a valid name, and cannot enter normal play until the dog and the whole starter document save together; visitors never see or mutate that owner-only gate. New farms receive exactly one growing plot, 20 Dog Food, and one seed from six randomly selected unique crop types. The first write pins that random pool, while normalization preserves spent supplies, released pets, cleared layouts, and old test farms without re-granting anything. The API trust boundary carries the onboarding marker and identifies a missing database row as new. Focused cache/account/visitor coverage plus the complete 120-test farm suite are green.

Dated history extracted from the root `CLAUDE.md` (2026-07-23) so that file can stay a lean orientation guide instead of an ever-growing log. This file is a curated narrative, not a replacement for `git log` — read it for *why*/*what shipped when*, not for line-level diffs.

## The Farm: Nothing Stands in a Doorway (2026-09-21)

Walking the buildings turned up one flaw in every one: a rail across the doorway (the stable's low half-timber rail, the farmhouse's mid rail, the shed's bottom rail), a slab through the room at waist height (the greenhouse's sill plate and top plate were drawn as full-footprint boxes, so a "floor" hung through the middle of it and a ceiling hid the glass roof), a solid disc through the tower (the silo's hoops and the windmill's ring beams were full cylinders), a plinth poking 8 cm into the barn's opening, and a window on the hinge side of the shed and coop doors that the open leaf swung straight through. One helper fixes the class: `wallBands()` in `farm-props-buildings.mts` draws a horizontal band as ONE BOX PER WALL off the same `shellWalls` list the walker reads — below the door's head it follows the jambs and so stops at the opening, above it the band runs the whole face — and every rail, plate, hoop and ring now goes through it; the foundation's jamb plinths reach past their outer corner only; the shed and coop windows moved to the latch side; the farmhouse's stoop is a flagstone landing no taller than the threshold, so the leaf swings over it instead of through it. `farm-fixtures.test.mjs` now builds every building with the stub THREE (which remembers each geometry's class and size) and asserts that **nothing but the door leaves overlaps the doorway volume** (hinges are tagged `doorLeaf`), so the next rail that crosses a door fails CI. All nine doorways were screenshotted head-on, open, headlessly. 78 farm tests green.

## The Farm: Real Materials on Every Model, and Pets You Carry (2026-09-21)

**No more flat colours.** `js/farm-materials.mts` is a procedural material library: fourteen kinds (lapped `planks`, board-and-batten `battens`, `shingles`, `clay-tiles`, `corrugated` iron, `fieldstone`, `plaster`, `brick`, `galvanised` steel, plain `wood` grain, `straw`, `bark`, `foliage`, `soil`), each drawn deterministically onto a 512 px COLOUR tile and a matching HEIGHT tile (a bump map), cached by kind and palette. **Tiles are in metres**: a material carries `metresPerTile` and the mesh helpers (`tbox`, `tcylinder`, `tsphere`, `tmesh`, `metricUvs`/`scaleUvs`) rewrite a geometry's UVs so boards, bricks and shingles are the same real size on a 7 m wall and a 0.3 m plank, and never stretch. Under node (no canvas) `farmMaterial` degrades to a plain colour, so the stub-THREE fixture test runs the builders unchanged. The room's `cylinder` primitive gained an optional shadow flag; nothing else in the room moved.

**Every building rebuilt on it** (`farm-props-buildings.mts`): a stone-plinth *ring* under the walls (the floor inside stays the floor, a low threshold at the door), rafter tails and fascia at every eave, windows with frames, muntins, sills and (on the farmhouse) louvred shutters and flower boxes, strap-hinged doors with handles, wall lanterns, interior studs and top plates. The **barn** is red board-and-batten under a gambrel roof with the gable over the doors, a hay-loft door with a hood and pulley, a louvred cupola with a rooster weathervane; the **stable** dark half-timbering over planks, dutch doors, a horseshoe, iron bars over the stall fronts, hay nets and a saddle; the **farmhouse** plaster with beams and braces, clay tiles, a stone chimney with a pot, a tiled porch awning, a board ceiling, a fire in the hearth; the **greenhouse** a brick plinth, gutters and a downpipe, a propped roof vent, tomato canes and seed trays; the **shed** rusting corrugated iron on a timber frame, a stovepipe, a hasp and padlock, jars and a vice; the **coop** a rooster vane, a wire-mesh window with a propped shutter, a feeder and fount; the **silo** corner seams, cap ribs and a vent, a caged ladder, a discharge chute, a plate under the dome; the **windmill** a fieldstone base course, timber braces, sails twice the size on a windshaft with a fantail, a hopper over the stone; the **gazebo** turned posts on a lattice skirt, brackets, a spindle frieze, a board ceiling. Props, fences, plants and ponds had the same pass: bark trunks with buttress roots, leaf-textured canopies and hedges, a fieldstone wall with no gaps and capstones on edge, barbed wire, a pond that is a lathed hollow with a grassy lip and cattails, a scarecrow with a stitched face and a crow, wagon wheels with felloes and spokes, a stencilled crate, ringed log ends, a fluted lamp post. Contact sheets of all 50 rows and in-world walks through the barn, stable, farmhouse and greenhouse were eyeballed headlessly.

**Pets can be picked up and put down** — so a pet can be carried into a stall. `farm-pets.mts` gained a `carried` state that rides the player's pose each tick (`CARRY_REACH` ahead, `CARRY_HEIGHT` off the ground, facing the player's way, taking no ground for the others), `pickUp` (never a swimmer), `putDown(spot)` (refused, and still carried, when a body its size does not fit), `canStand` and `carried()`. Two rules make a shut stall a pen: **wander targets are picked by line of sight** (`canSee` samples the segment for solids, so a pet never chooses a destination through a wall), and a pet already standing inside a building's keep-out box treats the box as its room (short strolls, keep-out relaxed) rather than a place to leave. `farm-interaction.mts` has `putDownSpot`/`findPutDownSpot` (straight ahead by the reach plus the species' radius, then a little further if that is taken — through a doorway) and the pick-up/put-down prompts. On the page E on a pet lifts it (tag off), E with a pet in hand sets it down where it fits (heart and a look back on the put-down), doors are still worked with full hands but an OPEN door yields to a put-down that fits (a shut one is opened first), ladders and seats need free hands, and build mode empties the arms (`dropCarried`). Bodies snap a carried pet to the hand rather than easing. Tests: the sim carries, refuses, places, and a corgi put in a stall with the door shut wanders three minutes without leaving it; the page test pins the seams. Verified headlessly end to end: picked up a corgi on the field, carried it to the stable, opened the doors, opened stall 1, put it down inside, shut the door, and it was still there. 77 farm tests, 475 shared frontend tests green (the same two pre-existing `/me` failures).

## The Farm: Stall Half-Doors Swing — Door Fixtures (2026-09-21)

**A door inside a building is a fixture with a hinge.** `farm-fixtures.mts` gained a fifth kind, `door`: a solid leaf hung on one end of its width (`door: { hinge, swing, reach, label }`), and the stable's three stalls are each a fixed front panel plus a 1 m half-door hinged on the partition beside it, swinging INTO the stall so the aisle stays clear. The rule that makes it honest: **an open leaf is still solid.** `fixtureDoorPose(entry, open)` (pure) says where the box stands — shut across the opening, or swung about its hinge by its own angle — and BOTH the obstacle list (`fixtureObstacles` now takes the open-door set) and the drawn leaf (`drawFixtureDoor`: a hinge group tagged with the fixture name, turned by the same `swing` through the swing controller extracted from `createDoorLeaves`) read it, so a door stood open against the partition is a thing the body walks round, never through. A test proves the open leaf overlaps no partition or panel and that the walker enters the stall only once it is open, at three rotations.

**One open-door set for every door.** `DoorRow` carries a `doorId` beside its `instanceId` — a building's or a gate's is its instance id, a fixture door's is `<instanceId>-<fixture>` (the same id its obstacle already carried) — and the page's `openDoors`, `world.doorsFor` and the prompt all key on it. `doorRows` lists a building's own door first, then its fixture doors, and the page now takes the **nearest** door in reach (`nearestDoor`) rather than the first, so standing in the stable's aisle facing a stall works the stall and not the doors behind you. Builders hand fixture doors back by name in `fixtureDoors` (`{}` for the other eight buildings and every prop); the world animates them beside the building's own.

Verified headlessly: walked in through the stable's doors, pushed against the shut stall door (stopped at the leaf, prompt "Press E to open the stall door"), E, walked into the stall, strafed into the open leaf standing against the partition and stopped at it; screenshots of the leaf shut and open. 73 farm tests, 471 shared frontend tests green (the same two pre-existing `/me` failures).

## The Farm: Nothing Superficial — Fixtures Collide, Ladders Climb, Gates Swing, Benches Seat (2026-09-21)

**What stands inside a building is data, and the body hits it.** `js/farm-fixtures.mts` (pure) lists every building's furniture in the building's frame the way `shellWalls` lists its walls — the barn's hay pile, workbench, loft, ladder and the hay up on the loft; the stable's stall walls and half-doors; the farmhouse table, stools and hearth; the greenhouse benches; the shed's shelves, bench and tools; the coop's nest boxes and roost; the silo's grain heap and spills; the windmill's millstone, shaft, sacks and loft; the gazebo's railings and bench ring — and the garden bench's seat. `farmObstacles` turns them into solids beside the walls, and the builders in `farm-props-buildings.mts` draw each one FROM the list (`fixtureNamed` → `drawFixtureBox`/`drawLadder`/`drawPlatform`/`drawSeat`/`drawRailing`), so the box the eye sees is the box the body stops at. A test builds all nine buildings with a stub THREE and asserts every fixture got a mesh at its own coordinates; another asserts no two fixtures share space, in the ground plane and in height. The shell geometry (`roundFace`, `shellWalls`, `buildingLocalToWorld`) moved to `farm-shell.mts` so the fixtures could import it without a cycle; `farm-scene.mts` re-exports.

**Every solid has a height, and the player has one too.** `FarmObstacle` carries `bottom`/`top` (a wall is `Infinity` tall), and `js/farm-body.mts` (pure) is the vertical body over the room's flat walker: WALKING on the ground or a `platform` (falling under `GRAVITY` when nothing is underneath — walk off the loft's edge and you land on the floor), CLIMBING a `ladder` (E takes hold from the climbing side at its foot, or from the platform at its top; W/S move the body; the top steps it off at the ladder's `exit`, the bottom at its `foot`; E lets go and gravity takes it), and SEATED on a `seat` (E sits at the nearest spot along the bench facing where the seat faces, eyes at `SEATED_EYE`; E or any move key stands the body back up where it was). `bodyObstacles` keeps only the solids the body's own span crosses, which is the whole trick: a body on the ground walks under the loft, a body on the loft walks beside the hay on it, and the walker never learns about height. Animals get `obstaclesForSpan(…, 0.05, 1.2)`.

**The three ladders go somewhere.** The barn's climbs to the hay loft (now at 2.2 m under a steeper 2.6 m roof rise so a standing player's eyes clear it — a test checks the geometry), the windmill's to a loft that did not exist before, and the silo's exterior ladder to a **catwalk ring** round the top of the tower with a rail broken only where the ladder lands — a view over the whole farm, and a long fall through the gap. The silo's interior rungs-to-nowhere are gone.

**The gate is a door.** `decor.fence.gate` is `solid` with `gate: { reach }`; its leaf swings on the buildings' `BuildingDoors` contract, `doorRows` lists it with `label: "gate"`, and `farmObstacles` drops its box while it stands open — so a fence with a gate in it pens the animals until somebody opens it. (The starter gate sits on the field's edge, so opening it is only a gesture there; place one in a pen and it matters.)

**The barn's bad bench.** The workbench sat flush against the ladder on the east wall; it is on the west wall's front half now, and a regression test keeps them more than four metres apart.

Verified headlessly (puppeteer + SwiftShader): walked to the barn, opened the doors, took the ladder, arrived on the loft at `y = 2.2`, walked off and landed, stopped at the workbench, plus a "peek inside" render of every building from its door and from each loft/catwalk. Tests: `farm-fixtures.test.mjs`, `farm-body.test.mjs` (a real climb, the silo catwalk, sitting, the gate), and the scene/page/interaction tests updated to the new contract — 70 farm tests, 468 shared frontend tests green (the two `arcade-mepage`/`arcade-playerpage` failures predate this change and belong to the uncommitted `/me` work).

## The Farm: Every Building Enterable, the Countryside, and a Catalog of 50 (2026-09-21)

**No superficial doors.** A building row now carries a `shell` (`js/farm-catalog/decor.mts`): `walls` (four box walls with a doorway cut in the +z face), `round` (eight flat faces on the footprint's inscribed circle, the doorway in face 0 — a silo, a windmill), or `open` (a post per corner, nothing else — a gazebo). `shellWalls()` in `farm-scene.mts` is the ONE description of a building's walls: `buildingObstacles()` hands exactly those boxes to the walker and the pet sim, and `drawShell()` in the new `farm-props-buildings.mts` draws exactly those boxes, so the doorway the eye sees is the doorway the body fits through. `createDoorLeaves()` hangs one or two leaves on the jambs from the row's `door` (`width`, `height`, `leaves`, `reach`) and returns the `BuildingDoors` the world animates; `doorRows()` carries the building's title and reach, so the prompt says "Press E to open the shed door" and a wide barn door is worked from further off than a coop's. The old `BARN` constant, `barnObstacles` and `barnDoor` are thin wrappers over the catalog's barn row. Nine buildings, all enterable and all dressed inside: barn (loft, workbench, hay), stable (three stalls with half-doors), farmhouse (rug, table, hearth with an ember light, hip roof from one BufferGeometry), greenhouse (glass walls on a brick plinth, benches of pots, glass doors), tool shed (shelves of tins, tools on the wall), a walk-in chicken coop (nest boxes, roost, pop-hole ramp), grain silo (hoop bands, ladder, grain heap), windmill (turning sails via the model's `animate`, millstone, sacks) and gazebo (railings on three sides, bench ring). A test drives every building at three rotations through the real walker: the shut door stops the body outside, the open door lets it to the middle, and a wall pressed from outside holds.

**The countryside.** `farm-scenery.mts` is everything the eye sees that is not on the layout: a ring of rolling hills the fog softens, a treeline of 120 cheap trees between the hills and the fence, nine self-lit clouds that drift, the sun disc on the directional light's bearing, and the ground cover — 2600 five-blade grass tufts and 320 wildflowers with stems as instanced meshes, seeded so the same farm looks the same every load, re-laid on every `sync(layout)` off `groundCoverExclusions()` (every placed box but the fences) so nothing pokes through a floor or a pond. Tree canopies are shaded clusters (lit crowns on top, deep shadows under, seeded so no two are twins). The hemisphere fill is a touch stronger and the sun a touch warmer.

**A catalog of 50.** Plants moved to `farm-props-plants.mts`; `farm-props.mts` keeps fences, water and props and the table that indexes all three files. New: split-rail, wire and hedgerow fences; birch, apple, willow, wheat, vegetable rows, lavender, stump; lily pond; doghouse, wheelbarrow, hay wagon, barrel, crate stack, log pile, campfire (flickering flame + light through `animate`), birdbath, signpost, mailbox, water pump, beehive. `FarmDecorModel` gained `animate` and the world ticks it beside doors. The server needed nothing: `decor.<cat>.<variant>` was already the whole policy. Verified with a contact sheet of all 50 thumbnails and a headless walk-through of the seeded showcase.

## The Farm: Build Mode, and the Editor Frame Lifted Into `js/space-editor/` (slice 3) (2026-09-21)

**The field is the player's now.** Everything that stood on the farm in slice 1 — the perimeter fence, the barn, the trees, the hay, the trough — is a catalog row on the layout (`js/farm-catalog/decor.mts`, 21 items in five categories: fences, buildings, plants, water, props) and the layout is version 2 (`decor` rows: `instanceId`, `itemId`, `x`, `z`, `rotationY`, `length`). A v1 document migrates by seeding `STARTER_FARM_DECOR`; in v2 an absent `decor` is the starter field and `[]` is a cleared one — the room's absent-vs-empty rule, and the server (`farm-loadout-catalog.mts`) stores the version it was sent so the client can migrate. Every prop is procedural (`farm-props.mts`, a builder per catalog `model`, CI-checked), `farm-world.mts` is now a decor runtime (build on first sight, rebuild on item/length change, re-pose on a move, dispose on removal), and `farm-scene.mts` derives obstacles, keep-out boxes, water regions and door rows from the rows. Doors are per building.

**B builds.** `js/farm-editor.mts` + `farm-editor-panel.mts` are the room editor's shape on the farm's own rules: a rail with Ground plus one tab per category, real rendered cards, a placed list, a floating inspector built once per selection and patched, drag/nudge/rotate/copy/remove, Ctrl+Z, presets, C to centre, Escape to deselect, Reset keeps the pets. **A fence is stretched from one end with the other anchored** — the room's end-arrow gizmos over pure `farm-decor-layout.mts` (`stretchFarmDecorEnd` bisects the length down to the longest that fits), a typed length grows about the middle, and a pulled or dragged fence end snaps to another fence's end with a guide (Alt bypasses). The placement rules are exact rotated boxes (separating-axis, not the room's bounding-box test) so a diagonal fence runs right up to the edge; fences cross fences freely so pens are easy; nothing solid may cover the gate spawn; a flower bed takes no space. A visitor never sees the button and B does nothing for them.

**Ponds unlock the swimmers.** A `decor.water.*` row is a `water` habitat; `farmHabitats(layout)` reads it, the adoption panel offers the shark, anglerfish and jellyfish once one exists, and the pure sim keeps them inside the ellipse inscribed in the pond's box (`insideWaterRegion`), below the surface (`hoverHeight` is negative for water species), and re-homes them when the pond moves. Ground animals treat ponds as solid; the last pond cannot be removed while a swimmer lives in it (`removeFarmDecor` → `habitat`), and a swimmer with no pond is dropped on load rather than drawn on grass.

**The frame is shared, not copied.** Three pieces left the room for `js/space-editor/`, and the room adopted them in the same change: `editor-camera-controller.mts` (orbit/pan/zoom/presets/eased transitions/view offset/far-plane swap around the pure camera maths — the room hangs its wall cutaway on `onPose`), `editor-history.mts` (pure bounded undo with open/close gestures, so a drag or a colour pad is one step and a no-op step is dropped), and `model-thumbnails.mts` (the offscreen renderer; the room's decor thumbnails are a wrapper on it). `css/space-editor.css` is the frame and widget vocabulary both pages link; `room.css` keeps only what the arcade adds (swatch patterns, card kinds, cabinet catalog, avatar tab) and `farm.css` only tints. The room editor went from 1277 to 1132 lines. Not lifted yet: the pointer gesture router (which button starts which gesture) — each editor still owns ~80 lines of it, the next seam if a third space appears. Verified by driving both pages headlessly (fence stretch by pixel-found handle, typed length, undo, pond → shark adoption, reload, visitor gate; the room's orbit/pan/zoom/presets/add/undo/drag/save).

## The Farm: A Barn You Can Enter, and the Pet Engine (slice 2) (2026-09-20)

**The barn is a building now, not a box.** The first barn's roof "was all wonky" (cone gables, slabs that did not meet, a hatch through the roof). It is rebuilt in `farm-props.mts` from `BARN`'s numbers: a plank floor, four walls with the doorway cut out of the +z one, two pitched slabs meeting on a ridge along x with the pitch derived from `roofRise`/`roofOverhang`, triangular gable fills extruded at each end, a lantern inside, and two door leaves hinged on the jambs that swing outward on `doors.setOpen` and ease over `update(dt)`. **The player can walk in**: `farmObstacles({ barnDoorsOpen })` (pure, `farm-scene.mts`) expands the barn into its walls, the two jambs and — only while shut — the door itself, all in the barn's rotated frame through `barnLocalToWorld` (the exact inverse of the walker's obstacle test, asserted). E at the door opens or closes it from either side (`farm-interaction.mts`: distance + facing, and inside 0.9 m facing is waived because a thin door plane makes the dot meaningless); toggling refreshes the obstacle list, so the shut door is solid and the open doorway is not. Driven headlessly by actual position (the sim runs slower than wall-clock under SwiftShader, so key holds cannot be timed — a read-only `globalThis.__farm` handle was added for exactly this): open → walk in → close from inside → walking out is blocked at the door line.

**Pets live on the farm.** `js/farm-pets.mts` is the pure sim (no three, no DOM, no clock, injected random) ticked at 60 Hz: `idle → wander → idle` with `attention` cutting in from outside — the hook every later command (call, treat, sit) interrupts into. A pet picks targets inside the field, off every solid thing and outside the barn's box (`keepOut`), walks with five feelers around obstacles, keeps out of other pets' radii and the player's `PLAYER_CLEARANCE`, and gives up a stroll it cannot finish; air species hover and bob. `sync(layout)` spawns new rows on a free spot and keeps known rows where they are, so a rename never teleports anybody. The test walks seven pets for four simulated minutes and asserts nobody leaves the field, enters a wall or stands in another animal; a pet never reaches the player; attention faces and holds. `farm-animal-clips.mts` cuts the pack's single 120-frame track into named clips (`AnimationUtils.subclip`, THREE handed in, stubbed in the test). `farm-pet-bodies.mts` draws: own GLB copy per body, **scaled to the species' `height` in metres** (the pack is ~400 units tall — the first render put a rhino the size of the field in the middle of the view, which is why `scale` became `height`), idle/walk cross-faded off `moving`, eased toward the sim pose per frame, a name tag, and a heart that floats up on E. `farm-pets-panel.mts` (DOM) adopts, renames and releases through three actions the composition root routes through one `persistLayout`; species cards are real portraits via `arcade-room-avatar-thumbnails.mts`, which gained an injectable `resolve` (asset URL + pose clip + framing) with the avatar default unchanged. P opens the panel (owner only; keys and the mouse belong to it while open), the three swimmers sit greyed at the end saying "Needs a pond". Verified headlessly: adopt three, rename one through the row, the layout and the device cache hold them, walk to the corgi, "Press E to pet Biscuit", heart, `attention`.

## The Farm: Environment (slice 1) (2026-09-20)

**A second personal 3D space at `/farm/`, built on the room's stack rather than copied from it.** Scope in `planning-docs/FARM_SCOPE.md`: a walkable outdoor farm where the player's pets (the Gobkit CC0 animal pack, `farm/assets/animals/`) will roam, built out later with fences, ponds, plants and a farming loop. This slice is the field itself. **Reuse was by import, not by fork**: the farm imports the room's placement maths, procedural surface renderer, decor primitives and generic store, and only two room files changed. `arcade-room-store.mts` became generic over the document — `createLayoutStore(spec, options)` takes a `LayoutDocumentSpec` (slug, cache key, normalizer, starter document) and `createRoomLayoutStore` is that call with the room's spec plus `loadSelfAvatarId`; the room's behaviour and tests are unchanged. The first-person walk was lifted out of the 834-line `arcade-room.mts` into pure `arcade-room-walker.mts` (`stepWalker`, `lookWalker`, `obstacleBlocks`, `forwardOf`; bounds and obstacles injected, 9 tests), and the room adopted it in the same change. **The world is procedural**: `grass` and `dirt` joined the surface pattern list (drawn with a new `wrapped` helper so organic tiles repeat without a seam — the first render showed the grid), `js/farm-catalog/ground.mts` is a pure five-row ground catalog on the room's `SurfaceStyle`, and `farm-props.mts` builds the barn, trees, hay bales, trough and post-and-rail fence from the room's primitives. `farm-scene.mts` (pure) holds the bounds (28 × 28 m), the spawn at the south gate, and the starter dressing as `FloorObstacle` rows — a test proved every piece stands inside the field and no two overlap, which caught the barn's rotated box reaching the hay bales. `farm-layout.mts` is the v1 document — `{ ground, pets: [{ instanceId, speciesId, name }], decor: [] }`, pets deliberately positionless because they will roam — with adopt/rename/release/ground rules and a normalizer; `js/farm-catalog/animals.mts` is the ten-species catalog with `habitat` on the row from day one so the three swimmers wait for a pond (`adoptableAnimals({ water: false })` = 7). Server: `services/farm-loadout-catalog.mts` on `game_loadouts` under `farm`, the room's policy exactly (namespace + bounds, client owns meaning, `decor` emitted only when sent), 8 tests. Entry chips on the grid, `/me` and `/player` beside the arcade chip, the player page's arcade-link wiring generalised to a `wirePlayerSpaceLink` seam. Verified headlessly (SwiftShader): gate → enter → walk → look → sprint into the fence clamps at the inset; visitor mode via `?id=`; the grid chip renders. Not yet: anything that answers E, build mode, pets on the field — slice 2 is the pet engine.

## Arcade Room: Text Chat (2026-09-20)

**Press Enter to talk to the arcade, the way an in-match chat works.** Enter opens a single-line box bottom-left, Enter sends and closes it, Escape throws the draft away, and while the box is open every key is the box's and none walks the player (a held W when the box opens is cleared, so nobody drifts under their typing). Closed, the log shows the last 12 s of talk and fades; open, it shows the whole log (60 lines), in the order it was said — the first cut appended faded lines back at the bottom on reopen, caught in the headless screenshot. Three layers: **the server** relays `arcade_room_chat { text }` to everyone else in the arcade with the sender's name as the roster knows it, 200 chars, whitespace collapsed, and refuses a burst with `TOO_FAST` (400 ms minimum gap, 8 lines per rolling 10 s) rather than dropping it silently; **presence** carries the words and nothing else (`sendChat`, `onChat`, `onChatRefused` — a line is not roster state); **`js/arcade-room-chat.mts`** is the pure state machine and log (Enter/Escape, fade, the bound, `self` lines drawn from the send because the server never echoes, and `system` lines for arrivals/departures read off the roster so a dropped socket is not announced as everyone leaving, and for a refused line); `arcade-room-chat-view.mts` patches the DOM by entry id and hands focus back to the canvas. Hidden in build mode and inside a cabinet. 5 chat tests + 2 presence + 2 bridge tests + a page-wiring test; driven end to end with two headless browsers against a local server (line both ways, cancel, order after fade, no walking while typing, "Bob came in." / "Bob left."). **A line is also a speech bubble over the speaker** (`visitors.say`): a second sprite above the name tag, white paper with a tail pointing at the body, word-wrapped by the pure `wrapChatBubble` (measure function injected, three lines then an ellipsis), held 6 s and faded out over the last 0.8 s; the log is the record, the bubble is who is talking. No touch entry point yet (no keyboard, no way in) — the obvious next step. Both repos deploy together.

## Arcade Room: Neon That Lights Its Whole Length, a Fuller Catalog, and Repaintable Surfaces (2026-09-18)

**Neon was a box with one point light in the middle**, so a stretched strip had a bright spot and dark ends — "super obvious that the neon stuff is half baked." A strip is now `buildNeonStrip`: a round glass tube on a dark channel with a cap at each end, a thinner **white-hot core** inside the coloured glass (`lighten` in the new `arcade-room-decor-primitives.mts`), and a **glow wash** — an additive, depth-silent gradient plane the length of the tube lying on whatever it is mounted to (the wall behind a wall strip, the floor under a floor strip, the ceiling above a ceiling one; `modelSpecFor` hands the strip its mount, the one thing a builder learns about placement), feathered at both ends. The wash is tagged `glowHalo` and `decorModelBounds` leaves it out, so the selection outline (now a `Box3Helper` over a box the editor fills itself) and the catalog thumbnails still frame the tube, not the light. **Lights are spread along the length**: `decorLightCount` asks for one source per `DECOR_LIGHT_SPACING` (2.4 m, at most 8), `decorLightOffsets` spaces them evenly with a margin, each shares the item's intensity (`1/√n`, never below half), and strip lights stand 0.38 m off their surface so the falloff spreads rather than burning a spot. The runtime's `decorLightAllocation` hands out a room-wide budget (`MAX_DECOR_LIGHTS`, 32) in layout order beside the old item cap — an item the budget cannot fully serve gets what is left, rows past it keep their glow. Any long lit item benefits (the fluorescent tube, the bar, string lights).

**The catalog roughly doubled (58 → 121)** and the builders for the new shelf live in `js/arcade-room-decor-props.mts` on the shared primitives, keyed by prop name; the model file's `prop`/`wall-prop` switches fall through to those tables, so a new item is a catalog row plus one table entry. Furniture: round table with stools, a stretchable trestle table, coffee table, high-top, sofa, armchair, a stretchable **bar** with underglow and a bottle shelf, a game shelf, a diner booth, lockers. Props: gumball machine, change machine, prize wheel, photo booth, foosball, a walkable dance pad, crates, barrel, neon palm, lava lamp, trophy case, balloons, traffic cone, water cooler. Wall: dartboard, extinguisher, cork board, whiteboard, security camera, AC unit, a **night window** with a lit skyline, a stretchable trophy shelf, light switch, air vent, and a sconce (Lighting). A new **Ceiling** category: ceiling speaker (a jukebox emitter like the other two), fan, a stretchable pennant banner, a **hanging sign** on chains that takes the player's words, projector, vent; plus new ceiling lights — chandelier, stretchable string lights, paper lantern, stretchable track lights, and a blacklight tube that also hangs on a wall. Signs gained a **marquee board** (backlit white letter board with bulbs) that takes the player's words; `marquee` and `hanging-sign` are the two non-neon text kinds, sized as boards that only widen when the words outrun them. A test reads every `case`/table name and asserts nobody names a prop nothing draws; every new model was rendered on a contact sheet and checked.

**Floors, walls, ceilings and trim can be repainted.** Every surface was already a pattern plus one-to-four colours, so painting is a slot-by-slot override: `RoomLayout.surfaceColors` holds per kind a list matched to the chosen finish's `colors` (a hex where the player changed it, `""` where the catalog stands, `[]` untouched — and painting a slot back to its catalog colour clears it, so the room compares equal to one never painted). `resolveSurfaceStyle` in the pure catalog writes them over the style — a self-lit neon grid glows in its new accent — and the shell re-dresses only the faces whose finish *or* paint changed. Picking a different finish clears its paint, because another pattern's slots mean other things. The Surfaces tab now leads each section with a **paint block**: a chip per slot (Base/Accent/Third/Fourth) showing the resolved colour, the same inline picker as decor bound to the pressed chip, and a Catalog colours reset — built once per (kind, finish) and patched, never rebuilt under a drag, and routed through the editor's preview/commit gesture so a pad drag is one undo step. Server: `surfaceColors` is shape-checked (hex or "", four slots, trailing empties trimmed) and emitted only when the client sent it, so old rows come back untouched. Driven headlessly: floor accent by hex, wall base by pad drag, one undo restoring the brick.

## Arcade Room: Presence That Actually Moves (2026-09-18)

**The shared room shipped frozen.** The first live report was "super jumpy and buggy: I pressed wave and nothing happened, then my character disappeared and appeared in a different place with a clone." Reproduced with two headless browsers: Bob's roster had Alice at `(3, 2)` while her body still stood at the spawn. `arcade-room-visitors.mts` took a member reference at `sync` time and read the pose off it every frame, but a pose event *replaces* the frozen member in the roster and never notified — so a body only moved when somebody joined, left, waved or reconnected, and then it teleported. Movement is now a **per-frame read**: `visitors.update(dt, now, presence.members())` reconciles and reads the latest pose every frame, and a pose event notifies only when `activity` changes (the chip's "▶ Bird Duty" is worth an event; footsteps are not). Around that, the rough edges the same session found: **the join carried `(0, 0)`** because the frame loop had not published yet, so everyone first appeared at the origin and slid to the spawn — the room now publishes once before `connect()`; **the keepalive rode the frame loop**, so a hidden tab went silent — it is a timer of its own (`setRepeating`, injectable); **a reconnect left a clone** until the transport noticed the dead socket — every page load mints a `sessionId` that rides on the join, and the bridge retires a member with the same player *and* session on another socket (`member_left`, reason `replaced`) while two tabs of one account (two sessions) still coexist; **the bridge now sweeps** members unheard for `STALE_MEMBER_MS` (20 s) in `tickActiveRooms`, and a swept client rejoins by itself on the `NOT_IN_ROOM` its next pose earns; `pageshow` reconnects a page restored from the back/forward cache. **Smoothness:** `js/arcade-room-visitor-motion.mts` is a new pure module (velocity from consecutive samples, the target run forward by the time since the sample capped at 150 ms, gait from measured speed with hysteresis between 3.0 and 3.5 m/s, a jump over 3 m is a snap not a glide) — the old walk/run choice compared the body's *gap* to 0.35 m and flickered every packet. A change of avatar keeps the old model standing until the new one loads instead of dropping to the capsule. **The wave:** the waver has no body to see it on, so the prompt now shows `👋 You waved at <name>` for 1.4 s the instant E lands. 21 presence/motion tests + 15 bridge tests; verified again headlessly (body follows the pose, join pose is the spawn, the wave reaches the other side). Both repos deploy together.

## Arcade Room: Everyone in One Arcade Sees Each Other (2026-09-18)

**The room was single-player in every sense** — two people on the same `/room/?id=` each walked a private copy of the layout — and the report that started this was exactly that: "i'm in the arcade with dewkybot but we don't see each other". Presence is the seam that makes it shared. `js/arcade-room-presence.mts` owns the protocol and the roster and nothing visual: one WebSocket to `factory-network-server` (`js/platform/api/factory-network-url.mts` now carries the prod/local rule every cabinet copied), an `arcade_room_join` for the arcade being viewed keyed by its **owner's** player id (owner and guests share one room; a signed-out player's own room has no id and no company, and is unreachable by anyone else anyway), poses at 10 hz sent only when they change plus a 2 s keepalive, quiet reconnect with backoff and the roster cleared meanwhile (a body standing after its socket is gone is a ghost). `js/arcade-room-visitors.mts` turns the roster into bodies: each member's own avatar GLB (loaded per body — the vendored three has no SkeletonUtils, and the HTTP cache makes the second load free), a capsule until it lands, a canvas name tag with `▶ <cabinet>` while they play, idle/walk/run from the pack's clips with `cheer_idle_a` as the wave, eased toward the latest pose rather than snapped. **Server:** `games/arcade-room/server/arcade-room-presence-bridge.mjs` is a self-owning bridge with no matchmaking — it claims only `arcade_room_*` frames, keeps `roomId → members`, relays to everyone else, sanitizes every field to bounded primitives, drops poses under 40 ms apart, caps a room at 12, and a disconnect is a leave; 13 tests including the router-contract scrape. **Interaction:** E on a person in reach waves (`findVisitorInReach`, pure, checked after cabinets and decor so the room's things keep E), the status line says who you waved at, and the other side sees the cheer. Arrivals fan out sideways from the spawn point (`spawnOffsetForCompany`) because everyone enters at the same spot. A `HERE NOW · n` chip lists the room; bodies hide in build mode. Found on the way: `readFactoryAccountSession()` carries no `playerId`, so the store's `selfId` was always `""` in production — `?id=<yourself>` never collapsed to owner mode and cabinets got `roomId: "local-arcade"` for every signed-in owner; the store now falls back to the factory profile's id (`selfPlayerId`, injectable). A guest's own body comes from `loadSelfAvatarId()` (account, then device cache, then default) since a visit never loads their layout. Verified with two headless browsers against a local server: roster both ways, poses, the wave prompt and the departure. Both repos deploy together.

## Arcade Room: Neon Strips Turn on the Wall (2026-09-17)

**A wall item may now turn in its wall's plane.** `DecorDefinition.spin.enabled` marks a wall item that is the same bar whichever way it hangs — only `decor.neon.strip` so far; signs and posters still always hang level. The turn is `spin` on the layout row (radians on one turn, 0 level, π/2 upright; wrapped by `clampDecorSpin` and again by the server catalog, and reset to 0 the moment the item leaves the wall, because off the wall `rotationY` is the turn). `rotationY` is untouched — the strip still faces the room from its wall, the model applies `rotation.set(0, rotationY, spin)` so the turn happens before the facing and the stand-off from the wall never moves. **The room a spun strip takes on the wall is its rotated bounding box** (`wallExtent`, pure): the edge clamp in `snapToWall`, the neighbour snaps in `alignWallPoint`/`alignStretchEnd`, and the duplicate step all read it, so an upright 3 m strip near the ceiling slides down rather than poking through it. `decorFrame` turns the handle axes with the spin, so the end arrows stand at the top and bottom of an upright strip and along the slant of a diagonal one; a vertical stretch snaps to the floor, the ceiling and neighbours' tops and bottoms (`alignStretchEnd` grew a `"y"` axis for wall items). Controls: Q/R and the toolbar rotate buttons step 15° (`rotateDecorItem` routes wall items through `setDecorSpin` instead of refusing), and the inspector has a **Direction · Horizontal / Vertical** row that sets it outright; the readout under the heading names the exact angle. Server: `normalizeDecorRow` passes `spin` through wrapped to one turn, a missing or junk value is level. Driven headlessly through vertical, two Q steps, a toolbar step, back to level, save and reload.

## Arcade Room: Your Own Words and Pictures, and Square Posters (2026-09-17)

**Custom text signs.** `decor.sign.custom-block` and `decor.sign.custom-script` lead the Signs category. A definition with `text.enabled` is a sign whose words the player types in the inspector (every keystroke previews, leaving the field commits, one undo step); the catalog's `text` is only the placeholder. The words live on the layout row (`text`, one printable line capped at 24 by `cleanDecorText`), and **the sign is as wide as its words** — `customSignWidth` is a per-glyph estimate in the pure catalog (no canvas there), `drawNeonText` still fits the line to whatever box it gets, and the text-sign canvas is now sized by height so a long line stays crisp. `setDecorText` re-places the item, because a longer line near a corner has to slide back. The placed list shows the words beside the title.

**Custom picture posters.** `decor.poster.custom` ("Your Picture") leads the Posters category as an empty frame with a drawn hint. The inspector's picture row goes through a new `/upload/poster` route (own Cloudinary folder `uploads/room-posters`, 1600 px limit, same auth gate and magic-byte validation as every other picture, and it answers with the picture's pixel size), `platform-api.mts#uploadRoomPoster`, and `arcade-room-store.mts#uploadPicture` — the store is the one place that knows only an account-backed owner can upload, so signed out the button is disabled and says to sign in, and the page hands the editor `null` rather than a function that fails. The row stores `image` and `aspect`; **the frame takes the picture's shape** inside the catalog square (a 2:1 photo is a 1 × 0.5 m poster) before the picture loads, and the layout is untouched until the upload lands. **A picture URL is accepted only under `https://res.cloudinary.com/<cloud>/image/upload/`**, on the client (`isDecorImageUrl`) and the server catalog alike, because every visitor's browser fetches whatever a room owner stored — the same namespace-only policy as item ids.

**Square posters.** Every `grid-previews/<slug>.png` is 1254 × 1254 and the poster frame was 0.9 × 1.2, so every game poster was stretched into portrait. The frame is now 0.9 × 0.9, and `arcade-room-custom-decor.test.mjs` reads each PNG's IHDR and asserts the frame's aspect matches its preview's, so a non-square preview added later fails CI rather than stretching. `decorExtent`/`decorFootprint`/`snapToWall` now take the row's finish (`{ length, scale, text, aspect }`, which a `RoomDecorItem` satisfies structurally) instead of two positional numbers.

## Arcade Room Editor: Resize by Handle, Snap to Neighbours (2026-09-17)

**The size slider is gone; size is set in the room.** Selecting a decor item now shows handles on it: a stretchable item (neon strip, runner, fluorescent tube, shelf) gets a big arrow at each END, a resizable one (signs, posters, rugs, lights, props, wall fittings) a grip at each CORNER. Dragging a handle **anchors the opposite end or corner** and moves only the side under the hand — grab the right end of a strip and pull it into the corner and the left end stays put — which is the gesture that runs a strip wall to wall. The maths is pure (`js/arcade-room-decor-resize.mts`): the handle positions come from a per-item frame (along / across / normal axes per mount), a stretch is the pointer's distance from the anchor along the item, a scale is its distance along the catalog-size diagonal, and **the limit is the room** — the requested size is clamped to the catalog range and then shrunk by bisection until the placement rules accept it exactly (no clamp, no overlap), so a strip stops at the wall's corner and a plant stops before a cabinet without either rule being restated. Strip and runner catalog maxima were raised to 20 m so the wall, not a constant, is the cap. The inspector keeps an exact typed number (`Length 4.50 m`, `Size ×2.50`) plus the `[`/`]`, `-`/`+` and Alt+wheel steps. The THREE side is a separate module (`arcade-room-editor-gizmos.mts`): handles are built once and re-posed, held at a constant screen size by camera distance, lifted off the surface by the item's normal, picked before the item under them, and the pointer is projected onto the item's own plane (the wall face, the floor, the ceiling) for the whole drag; a handle drag rides the existing preview/commit path, so it is one undo step and a refused growth holds the last good size.

**Alignment guides.** Every move and stretch is snapped through `js/arcade-room-decor-align.mts` (pure): an item's edges and centre are compared with every neighbour on the same surface — floor items and cabinets against each other, wall items against items on the *same wall* (along the wall and in height), ceiling against ceiling — plus the room's wall inset, corners, floor, ceiling and centre line (centre-to-centre only, so an edge never snaps to the middle of the room). Within the threshold the item jumps onto the line and a world-space guide segment (through both items, padded, lifted 2 cm off the surface) is returned; the editor draws exactly the guides it is handed as cyan lines with end dots and clears them on release. The threshold is 2 % of the camera distance clamped to 6–30 cm, so "close" means the same few pixels at any zoom; **Alt** while dragging switches snapping off. Cabinet drags snap too. Verified headlessly: end arrows found by pixel colour and dragged (2 m → 9.1 m, left end fixed; dragged off-screen → stopped at the wall corner), a poster's top-right grip dragged to ×2.5 with the bottom-left corner fixed, a typed 4.5 m, and a second strip dragged to within a few pixels of the first showing the guide mid-drag and reading 2.8 m on release. 20 new pure tests.

**Two models rebuilt** (`arcade-room-decor-model.mts`): the claw machine (kick plate, prize chute, coin slot, slanted control deck with joystick and lit button, chrome-posted glass case over a starry back panel, two layers of plush balls, gantry rails, carriage, cable and three-prong claw, a `CLAW` marquee ringed with bulbs) and the popcorn cart (striped skirt on every side with brass trim, bag dispenser and scoop, cream counter, brass-posted case with a serving hatch, hanging kettle with lid and crank over a popcorn mound and loose kernels, warmer tube, scalloped striped valance under a red roof with the `POPCORN` sign, two big spoked rear wheels on an axle, a front caster and a push handle).

## Arcade Room Editor: The Build Camera Stops Fighting the Player (2026-09-17)

**The build-mode camera was refactored around one model: the player moves a point on a plane just above the floor, and everything else follows it.** The complaints were concrete — the camera was clamped inside the walls so a side or corner view ended up pressed against a wall with nothing else on screen, cabinets were kept 40 cm off the wall by an oversized placement inset, and the orbit target sat at the centre of the full canvas, which is under the build panel. `js/arcade-room-camera.mts` stays pure (every rule below runs under node) and now does: (1) **zoom to the cursor** — multiplicative, and the camera/target pair is scaled about the cursor's hit on the target plane so the thing under the wheel stays under the wheel; (2) **grab-pan** — a right/middle/Shift-drag keeps the floor point it grabbed under the cursor exactly, at any pitch or zoom, falling back to the old screen-delta pan only when the ray grazes the horizon; (3) **focus** — `C`, or picking an item from the panel list, eases the view onto it and comes in to working distance; (4) **eased transitions** for presets and focus, shortest way round on yaw, cut short by any gesture; (5) **an off-centre projection** (`camera.setViewOffset`) so the target sits in the middle of the strip the panel leaves free, with the walking camera getting the plain centre back on finish; (6) **a position-based cutaway** — the lens may stand outside the room, and each wall it is beyond (or within 60 cm of) becomes a **ghosted pane** (transparent, no depth write) rather than vanishing, so the room keeps its outline and a wall item dropped on it still has a surface to read against; from inside, every wall stays solid exactly as when walking. The walking fog (27 m) and far plane are lifted only while building, because a lens 22 m outside a 20 m room otherwise saw fog colour. Cabinet placement now allows the padded footprint within 2 cm of the wall’s inner face. Verified headlessly across every preset, a max zoom-out, a pan to a corner, an orbit from there, zoom-at-cursor, grab-pan and a list pick.

## Platform Achievements, and Lovers Lost Is the First Registered Game (2026-09-17)

**The platform now has an achievement system.** Ownership rows live in `player_achievements` (migration 048, generic on `game_slug`, one row per player/game/id, so unlocking is idempotent by construction); definitions are code, not rows. `services/achievement-catalog.mts` is the registry — a game is one entry carrying its definitions, a `normalizeRun` plausibility gate and a pure `detect` — and `presentAchievement` is the one place a locked secret becomes `???`. There is deliberately **no "unlock id X" route**: a client describes a run (`POST /achievements/:slug/runs`, self-only) and the server's detector decides, running to a fixed point so a cumulative or meta achievement earned *by this run's own unlocks* lands in the same response. A repeated `runId` replays the stored verdict (`game_achievement_runs`), so a retried request hears the same unlock list and nothing fans out twice; submissions for one player+game are serialized on an advisory lock. Reads are public (`GET /players/:id/achievements`), the same asymmetry as the leaderboards. Both tables are on the account-deletion path. Trust boundary and the registration recipe: `planning-docs/ACHIEVEMENTS.md`.

**The frontend half is shared.** `js/platform/achievements/`: an API client on the shared platform client, a pure unlock queue (dedupe by `game:id`, so a replayed response shows nothing twice), and ONE toast for every game (injected styles, top-centre clear of the bottom-anchored touch controls, queued, auto-dismiss, tap to hurry, reduced-motion aware). A cabinet integrates in two lines: `createAchievementReporter()` at boot, `reportRun(slug, run)` at run end; signed-out play files nothing and the game keeps no unlock state. The trophy case is a new page, `/achievements/?id=<playerId>` (chips on `/me` and `/player`, which now stack vertically so three chips stay clear of the session nav), rendering each game's tree off `parentId` in CSS flex columns that collapse to one column on a phone; locked secrets show `???`. Verified headlessly at 1280 and 390 wide, plus the toast over the Lovers Lost canvas with touch pads forced on.

**Lovers Lost is the first registered game: 23 achievements** (progression, co-op, mastery, precision, score, speed branches, two secrets, and `Lovers Never Die` on an exact list of the 20 non-secret ids). Getting there required real gameplay fixes, each found by simulation rather than by eye: (1) **Perfect on spikes was impossible above ~speed 12** — the window was centred on the spike's position, but a jump needs two ticks of rise before the first tip is under the feet, so the latest *safe* jump is `2·distPerFrame(speed) − 1.16` units ahead of it; spikes are now graded against that reference, and the constant is checked against the collision sim at seven speeds and both sides (the girl's contact geometry is one pixel tighter and sets it). (2) **Birds could never be Perfect** — every clear was hard-coded Good; the crouch's start distance is now graded against the bird's window like the spike's jump start. (3) **Every Perfect window is at least two ticks wide** at the player's speed (`PERFECT_MIN_TICKS`), because a window narrower than a tick of travel can fall between two frames; a reachability test proves Perfect is hittable at every speed and phase for spikes and birds. (4) A one-jump spike chain inherits the jump's grade. The one place a grade becomes state is `resolveObstacleGrade` (score + `player.stats` tallies by grade and type + warmup counts), so scoring, feedback, the online snapshot and the claim cannot disagree; `finishFrame`, `crouchStartDistance` and `stats` ride the player and the snapshot. `run-telemetry.js` builds the run summary with `ownedLanes` (solo side / both / the online chair) so a partner's perfect lane can never land on the wrong account. `tests/achievement-contract.test.js` pins the server's mirrored constants to the game's and drives a **real full course** through the sim with a perfect-timing bot (crouch-cancel included), then through the server's normalizer and detector: 0 Goods, 0 Misses, Perfect Run unlocks. Cabinet 346 tests, API 846, shared frontend +15.

## Arcade Room: Speakers Relay the Jukebox, and a House Record Greets Every Visitor (2026-09-17)

**Speakers play whatever the jukebox is playing.** `decor.prop.speaker-stack` and `decor.wall.speaker` are now emitters (`SPEAKER_ITEM_IDS` / `isJukeboxEmitter` in the pure jukebox catalog). A speaker never plays on its own: while a record is on, the volume where the player stands is the LOUDEST emitter (`jukeboxGainAt`), never the sum, so two speakers side by side sound exactly like one and a wall of them cannot become a volume knob. A picked record still comes from the box it was picked on (plus every speaker) and still stops when that box is deleted; speakers alone cannot keep it going.

**The house record.** The layout document gained `music.defaultTrackId` (client normalizer refuses ids the catalog does not carry; the server catalog checks the `<game>.<track>` shape only, same namespace-only policy as decor). It starts on the first "Enter the arcade" click — the gesture autoplay wants — for owner and guest alike, and plays through every jukebox and speaker on the floor, or everywhere when the room has neither, so a host who set one is never met by silence. It has no source box: deleting a jukebox never stops it, and picking a record at a box takes over from it. The jukebox page carries an owner-only strip ("Play this when guests arrive" / "Clear") that posts `set-default` / `clear-default`; the room accepts those only when `canSetDefault` (owner mode), routes them to `roomEditor.setDefaultTrack`, which commits and **saves immediately** — it is the one layout change made while walking, with no Save button in sight. Every jukebox the record is coming out of pulses its tubes.

## Arcade Room: The Jukebox Plays Every Cabinet's Soundtrack (2026-09-17)

**The jukebox model was half a dome.** `CylinderGeometry`'s arc starts on its own +Z, so a half arc from 0 rotated a quarter-turn covered the box's *right* side and the top-left was open air. Rebuilt as a 1950s bubbler (`buildJukebox` in `arcade-room-decor-model.mts`): a half arc that starts a quarter-turn back and is tipped back onto the body top, two tinted bubble-tube arches running down chrome pilasters, a lit title window with a record on the turntable, a row of selector keys, a slatted speaker grille. The tube meshes carry `userData.jukeboxGlow` so the room can breathe them while a record plays (`pulseJukeboxGlow`).

**It is interactive, and the room is the player.** `decor.prop.jukebox` gained an `interaction` (the same seam the calendar uses) opening `room/jukebox/index.html`, a record list grouped by cabinet. The page only *picks*: it posts `{ action: play|stop|next|previous|toggle }` to the room and renders whatever state comes back, so the song keeps going after the overlay closes and the player walks the arcade. Three rules worth keeping: (1) **the box the player opened is the source** — `attach(instanceId)` at E, the volume falls with the distance to that row (`jukeboxGain`, full inside 2.5 m, silent at 16 m), and deleting that box in the editor stops the song; (2) **a cabinet game holds the record** — `openCabinet` → `suspend()`, `closeCabinet` → `resume()`, and the state still says "playing" because the pause is the cabinet's, not the player's; (3) commands are accepted only from the overlay frame's own window at the page's origin, and the page takes state only from its parent. Opened standalone (no parent window) the page runs the same `createRoomJukebox` itself, so there is one list and one player.

**The track catalog is pure and CI-checked.** `js/arcade-room-catalog/jukebox.mts` lists 44 records across 15 cabinets (site-root-relative paths, grid order); the catalog test asserts every file exists on disk, ids are unique and slug-prefixed, every cabinet with a soundtrack on disk is represented, and the message guards reject unknown records. Speed Demon's `my-radio/` folder is licensed commercial music and is deliberately not on the jukebox. A "Now playing" chip sits bottom-left of the room while a record is on.

**Interactive decor is findable.** The calendar was always in Decor → Wall, but last in the list under a category chip row that scrolls — interactive items now lead their category, and their cards carry a `PRESS E` badge. Verified headlessly: place, walk up, prompt, E, pick Hustle, next, Escape, walk off — chip stays, prompt hides. 116 room tests green.

## Arcade Room: Fullscreen Play, an Interactive Calendar, and a Second Surface Pass (2026-09-17)

**Cabinet games can go fullscreen.** A `Fullscreen` button sits beside `Step away` while a cabinet is open. It toggles `is-fullscreen` on the play layer and asks for real browser fullscreen on that layer (`requestFullscreen`, best effort); the layout — the game fitted to the whole viewport at its own aspect over a black backdrop — never depends on whether the API said yes, so an embed that refuses still gets a full-window game. `playScreenRect` in pure `arcade-room-screen.mts` decides the frame's rectangle either way (cabinet-screen projection or viewport fill, same aspect fit), and the render loop calls it every frame. `fullscreenchange` catches the browser's own Escape; the room's Escape steps out of fullscreen first and only a second press leaves the cabinet; `closeCabinet` always drops fullscreen so the room never comes back black. Verified headlessly: real fullscreen granted, Lovers Lost fitted to 1400×787 (16:9), exit restores the cabinet screen.

**Decor can be interactive: the Yam Bowling calendar.** `DecorDefinition` gained an `interaction` seam — `{ prompt, url, title, radius, facingThreshold }` or null — and a `calendar` model kind. `decor.wall.yam-calendar` hangs the real 2027 cover (`assets/calendar/cover.webp`) as a closed 11×8.5 print at twice life size (scalable ×0.5–3): paper stack, wire-o binding with loops, hook and wall pin, all procedural. Walking up and pressing E (pure `findInteractiveDecor` in `arcade-room-interaction.mts`, the same reach rules as a cabinet — a wall item's local +Z is its facing, a cabinet in reach wins) opens the page in a centred overlay (`arcade-room-decor-overlay.mts`) rather than projecting it onto the object, because a hand-sized calendar wants the whole screen. The page is new: `games/yam-bowling/calendar/viewer.html` + `viewer-embed.mjs/.css`, the calendar's own `createCalendarViewer` with the shop stripped away and a `target="_blank"` preorder link, sized from height so the open spread never scrolls. Two details worth knowing: (1) the frame holds focus while open, so Escape is listened for on the frame's *own* window (same-origin; a foreign page still has the button), and closing sets `src` to `about:blank` so nothing runs behind the room; (2) `decorCardImage()` in the pure catalog is now the one place that says "this item is its own picture" — the panel card and the thumbnail renderer both ask it, instead of each special-casing posters. No server change: the id is already inside the `decor.<category>.<variant>` namespace.

**Surfaces, second pass.** Six new procedural patterns — `herringbone` (rotated parquet), `marble` (blurred wandering veins plus hairlines), `hazard` (wrapped diagonals), `subway` (running-bond with a glaze highlight), `tartan`, `circuit` (right-angle traces between glowing pads) — and the catalog grew from 55 to 148 rows: floors 20→49 (more checkers incl. a big-tile one, six carpets, cherry/ash/ebony planks, two herringbones, three marbles, hazard, steel deck, violet/lime neon grids, circuit, starfield), walls 20→56 (eight paints, whitewashed/tan brick, four subway tiles, marble, more pinstripe/chevron/harlequin, a dark Memphis, two tartans, three wainscots, copper panels, painted cinderblock, hazard, three neon grids, three circuit boards, starfield), ceilings 7→21, trims 8→22 (copper, gold, gunmetal, rose gold, crimson, teal, cream, walnut, whitewash, five more neons). Trim deliberately stays `solid` and the catalog test now asserts it: trim is drawn on thin columns and skirting with a unit span, so a tiled pattern would smear. Verified headlessly: herringbone, subway, black marble, pink circuit, copper panels, starfield-violet and gold/neon-yellow trim all render.

## Arcade Room Editor: Inline Colour Picker, Real Thumbnails, Resizable Decor (2026-09-17)

**The colour wheel stopped closing.** The inspector used the browser's `<input type="color">`, and every colour change re-rendered the inspector with `replaceChildren`, which destroyed the input the native popup was attached to — so the popup closed on the first click and nothing could be dragged. Two fixes, both structural. (1) `arcade-room-editor-panel.mts` now builds the inspector **once per selection** (keyed by instance id) and *patches* it on later renders — labels, pressed states, slider values, picker value — so no widget is ever torn down under the pointer; the same bug was silently breaking slider drags. (2) The native picker is replaced by an inline one, `arcade-room-color-picker.mts`: a saturation/value pad, a hue strip, a hex field (accepts `abc`, `#abc`, `aabbcc`) and the nine neon presets, with pointer capture, keyboard arrows, and a grey-keeps-its-hue rule. Its maths lives in pure `arcade-room-color.mts` (hex ↔ HSV, tested under node).

**Preview vs commit.** Panel actions for colour, length and scale now carry an `EditPhase`. A `preview` (every pointer move of a drag, every `input` of a slider) opens a gesture — pushes one undo step — updates the working layout and redraws the room, coalesced to one scene render per frame; the `commit` (pointer up, slider `change`, a preset, a typed hex) closes it, drops the step if nothing changed, and lets the panel catch up. One drag is one undo, and the room recolours live while dragging.

**Catalog cards show the real thing.** `arcade-room-decor-thumbnails.mts` renders every catalog definition once through the same `createDecorModel` the room uses, into an offscreen 176×128 WebGL canvas (wall items near head-on, floor props three-quarter, rugs from above), and hands the panel a data URL — the panel stays THREE-free by taking a `thumbnail(definition)` function. Posters keep their grid preview. The neon shape and text drawing moved out of the model into `arcade-room-neon-art.mts` so the card, the thumbnail and the wall texture are one drawing.

**Decor is resizable.** A decor row carries `scale` (1 = catalog size); `DecorDefinition.scale = { enabled, min, max }` per category — signs and neon shapes to ×3 (the big neon joystick), posters and rugs ×2.5, lights and wall fittings ×2, props and furniture ×1.6. The rule, asserted in the catalog test: an item is **stretchable (length) or scalable, never both**, so it has one size control. `decorExtent`/`decorFootprint` take the scale; `snapToWall` keeps a scaled sign on the wall; `setDecorScale` re-places the item and refuses a size that would grow a solid prop into a neighbour; duplicate copies the size and re-fits; the walking player sees the scaled footprint. The model builds at catalog size and scales the group (`centred.scale.setScalar`), so every builder's hard-coded thicknesses grow with the item; the runtime's rebuild signature includes scale. Server: `scale` is bounded (0 < s ≤ 5, else 1) and emitted on every row. UI: a Size slider with a live `×1.50 · 0.9 × 1.2 m` readout, `-`/`+` (and `[`/`]`) keys, Alt+scroll over the room (notches within 350 ms are one undo step), and an "as big as it goes" message at the limit.

**Panel ergonomics found while verifying.** The inspector heads the same scrolling column the catalog lives in, so clicking a card far down left the new item's controls out of sight above — a fresh selection now scrolls the inspector to the top of the column. There was no way to deselect: the inspector has a × button, a still click on empty floor deselects (a moving one still orbits), and Escape deselects first and only then leaves build mode. The intro copy was cut to one line and the shortcut legend collapsed into a `<details>`, which is what makes a full inspector (colour + size + tools) fit without scrolling at 900 px tall. Verified headlessly with SwiftShader: pad/hue/slider drags register as one undo each, typed hex and presets apply, all 54 non-poster cards get a rendered picture, the ×3 joystick hangs on the wall at 1.8 × 2.4 m. 94 room tests, 816 API tests green.

## Arcade Room: Surfaces, Decor and a House-Flipper-Style Editor (2026-09-17)

**The room shell is data now.** `js/arcade-room.mts` used to hardcode the floor, wall, ceiling and trim materials plus three neon bars and two point lights. `js/arcade-room-shell.mts` builds the meshes once and `applySurfaces(surfaces)` re-dresses them from the layout, so a floor swatch in build mode changes the room under the cursor. The three neon bars became starter decor rows on the north wall (movable, recolourable, removable) and the cabinet glow lights are now children of the cabinet models so they travel with a moved cabinet. The placement grid only shows in build mode.

**Layout document version 2.** `{ version: 2, surfaces: { floor, wall, ceiling, trim }, items: [cabinets], decor: [...] }`. A decor row is `instanceId, itemId, x, y, z, rotationY, mount (floor|wall|ceiling), wall (north|south|east|west|""), color (#hex or ""), length (m or 0)`. Version 1 rows upgrade in place to the starter finishes and starter neon. The one subtle rule, mirrored on both sides: starter decor is seeded only when the `decor` key is **absent**; a version 2 row with `decor: []` is a room the player deliberately stripped and comes back stripped. The server (`services/arcade-room-loadout-catalog.mts`) keeps its namespace-and-bounds-only policy — `floor.<name>` / `wall.*` / `ceiling.*` / `trim.*` per slot, `decor.<category>.<variant>`, y ≥ 0, length ≤ 20, 96 decor rows max — and drops a bad row rather than failing the save. 5 new catalog tests; 816 API tests green.

**A pure catalog layer.** `js/arcade-room-catalog/` (surfaces, decor, inventory) has no THREE, DOM, canvas or storage, imports nothing outside itself, and is validated under node — the same pattern as Shark Hall's `scripts/cosmetics/`. 20 floors, 20 wall finishes, 7 ceilings, 8 trims, and ~70 decor items: neon strips (wall/ceiling/floor, tintable, 0.5–8 m), six neon shapes, eight text signs, a poster for every grid cabinet printed from its `grid-previews` image, four rugs, five lights (spot, pendant, disco ball, fluorescent tube, floor lamp), five furniture pieces, nine props (plant, trash can, stanchion, jukebox, vending, claw, pinball, popcorn cart, speaker stack) and seven wall fittings. Ownership is `createRoomInventory({ grantAll: true })`: everything is granted, the un-owned path (swatch disabled, add refused, unknown surface falls back to the starter) is written underneath so progression is a grant list, not a rewrite.

**Everything is procedural.** `arcade-room-surfaces.mts` draws 21 named patterns (checker, planks, tiles, hex, carpet, the classic galaxy arcade carpet, concrete, diamond plate, neon grid, terrazzo, stripes, chevron, brick, cinderblock, Memphis, harlequin, wainscot, acoustic tile, steel panels, starfield) onto a 512² canvas tile with a seeded generator, repeated per 20 m so a style reads the same on a wall and a floor; a test asserts every catalog pattern has a drawer. `arcade-room-decor-model.mts` builds every prop from boxes, cylinders, spheres and canvas planes, centred on its bounding box so one `mountOffset` places it for any mount. `arcade-room-decor-runtime.mts` syncs scene objects to the decor array by a finish signature (rebuild on item/colour/length/mount change, reposition otherwise) and allocates real point lights to the first `MAX_LIT_DECOR` (16) lit items — beyond that an item keeps its emissive glow but no light, because forward-rendered point lights are the one thing that can stall the room.

**Placement is pure and mount-aware.** `arcade-room-decor-layout.mts`: floor items clamp to the room, rotate freely on a 15° snap and refuse to overlap another solid (rugs are not solid); wall items snap to the nearest wall's inner face, slide within the corners, clamp vertically so the whole item stays on the wall, and always face the room (rotation refused); ceiling items hang from the ceiling plane. `floorObstacles` in `arcade-room-layout.mts` is the single list — visible cabinets plus solid floor decor — that cabinet placement, decor placement and the walking player all consult. Add walks outward in rings when the drop spot is taken; duplicate places a copy one width over with the same finish. 11 new placement tests, plus the layout tests extended for v2.

**The editor is tabbed and has an inspector.** `arcade-room-editor.mts` (input + state) and the new `arcade-room-editor-panel.mts` (all DOM). Tabs: Cabinets (as before), Surfaces (four grouped swatch grids with CSS-approximated pattern chips, click = live preview), Decor (category chips → catalog grid → click adds near the camera target and selects it; an inspector with Mount buttons, nine neon tints plus a free colour input, a length slider, Duplicate and Remove; and a Placed list). Dragging picks the visible surface under the cursor that the item can mount on, preferring its current one — the ceiling plane is invisible with the roof off, so the inspector's Mount buttons are how an item gets up there rather than a drag landing on it by accident. Arrows nudge camera-relative on the floor and slide/raise on a wall; Q/R rotate; Delete removes; Ctrl+D duplicates; **Ctrl+Z undoes** (40 steps, one per drag gesture, no-op drags dropped). Verified headlessly with SwiftShader screenshots: surfaces swap live, all decor kinds render, cabinet/prop/wall-sign drags land where the pure rules say, undo restores. 84 room tests green.

## Arcade Room: Account-Backed Layouts, Visiting, Camera Controls (2026-09-16)

**A player's arcade room now lives on their account.** `/room/` used to keep its cabinet layout in one browser's `localStorage`, which is a room nobody else can walk through. It now rides on the generic `game_loadouts` table under the `arcade-room` slug — `platform-api/src/services/arcade-room-loadout-catalog.mts` is the fifth catalog registered in `db/game-loadouts.mts`, so there is no migration and no route code. The catalog bounds the document (32 items, `cabinet.<game>.<variant>` id namespace, finite coordinates in a ±12 box, rotation wrapped to one turn) and drops a bad item rather than failing the save; it deliberately does not mirror the client's footprint table, and a player with no row gets `items: []`, which the client's own normalizer turns into the starter room, so a starter-layout retune stays a one-repo change. Unlike the cabinet garages the public read is the *whole* layout, because a room exists to be visited.

**The client has a store seam, and the editor no longer touches storage.** `js/arcade-room-store.mts` decides once whether the page is the owner's room or a visit, loads account → device cache → starter, writes the cache before pushing, and reports the true target of every save (`account` / `device` / failed push kept on device) so the editor's status line can say "Saved to your account" or "Saved on this device only. Sign in to keep it" rather than one message for both. The cache is keyed by player id so a shared browser never shows one account another's room. `createRoomEditor` now takes `initialLayout` + `persist` and nothing else; the page test asserts the editor has no `localStorage` in it.

**Other players' rooms are visitable.** `/room/?id=<playerId>` reads their public loadout and profile name, titles the page "<Name>'s Arcade", swaps the edit button for a "View profile" link, drops the B hint, and refuses build mode (`canEnter` and the store's `save` both say no). Every cabinet still plays. A link to your own id collapses to owner mode. Reachable from a "My Arcade" chip on the grid and on `/me`, and a "Visit Arcade" / "My Arcade" chip on `/player` wired by `wirePlayerArcadeLink`.

**Build mode has real camera controls.** `js/arcade-room-camera.mts` is a pure orbit-camera state (no THREE): left-drag on empty floor orbits yaw *and* pitch, right/middle/Shift-drag pans the target across the floor, wheel zooms, and six preset views (Overview, Front, Left, Right, Back, Top — `O`/`F`/`T` keys) sit above the cabinet list. Found while verifying in a browser: the overview and top presets sit above the 4.8 m ceiling, so build mode now lifts the roof off (dollhouse style) and the pose clamps the camera inside the walls so a zoomed-out corner view cannot end up behind one. Verified headlessly with SwiftShader screenshots of each preset and of visitor mode. 8 new catalog tests, 9 store tests, 9 camera tests; 811 API tests green.

**Cabinets can be hidden, not only placed.** Before this the normalizer re-seeded any missing starter cabinet, so a room could never have fewer cabinets than the catalog. A layout item now carries `hidden: boolean`: a hidden cabinet stays in the document with its placement (so it is not re-seeded and comes back where it was), takes no floor space, and has no placement as far as walking, playing, picking or the cabinet count are concerned. `setItemHidden` in `arcade-room-layout.mts` is the pure seam — showing tries the remembered spot, then the starter spot, and refuses when both are taken rather than stacking. The server normalizer passes only a real `true` through. Build mode gets a Hide/Show toggle beside each cabinet in the list, the `H` key on the selected cabinet, and a greyed row for hidden ones; Reset restores everything. 812 API tests green.

## Online Departures: Mini Hoops Forfeits, Rematch Refusals, Results-Screen Friend Status (2026-09-16)

**An opponent who quits a Mini Hoops duel now forfeits it at once.** The timed duel and HORSE both use the server's pause-then-forfeit model (`applyDisconnect` marks the seat disconnected and pauses; a second call forfeits), but unlike Shark Hall and Yam Bowling they lacked the guard that recognises an *explicit* leave — the generic `leaveLobby` calls `applyDisconnect` exactly once, so the remaining player was left at a paused court with every shot refused until the clock ran out. Both lobby games now check `lobby.members.has(clientId)` and settle immediately when the socket is genuinely gone. The timed-duel client also learns about `lobby_player_left` (the HORSE and tic-tac-toe clients already did), keeps that notice across the lobby refresh that follows it, refuses pulls while the server says `paused`, writes "reconnecting…" on the opponent's strip name, and titles a forfeit as one on the results card instead of as a score line.

**A rematch can be refused, and the screen says so.** Neither Yam Bowling nor Shark Hall had a decline: a rematch starts when both seats ask, and the only way to say no is to leave — which the server reports as `lobby_player_left` and nothing else after a decided match, and which both clients ignored, so "Rematch requested. Waiting for your opponent…" hung forever. Both clients now carry `opponentLeftClientId` on the snapshot (cleared when the room fills again), a pure `games/yam-bowling/online/rematch-status.mjs` decides the results line (opponent left / opponent wants a rematch / waiting / nothing) from the server's `rematchRequestedBy` rather than from what this device pressed, and Shark Hall's match wrapper emits `opponent-left` so the result card's subtitle changes under the winner's name. Neither sends a request into an empty room. Puck'd Up already handled this server-side and Bird Duty, Echo Duel, Hide and Seek and Build Buddy settle a departure without pausing, so nothing else needed the guard.

**Results screens no longer offer to add a friend you already have.** Battleshits and Lovers Lost rendered "Add Friend" beside the opponent for any signed-in viewer. The verdict now comes from one shared helper, `js/platform/ui/opponent-friend-status.mts`, which reads the viewer's own factory relationships (`friendPlayerIds`, the same list `/player` uses) and returns add / friends / none; an already-befriended opponent shows a quiet "Friends ✓" badge. A failed relationships read degrades to offering the link rather than hiding it.

**Battleshits' falling piece is always on top.** `.board-grid` is its own stacking context (`z-index: 1`), so the pending cell's `z-index: 10` could never lift the dropping piece past the bowl rim (`2`), the emote bubbles (`5`) or the sunk flash (`10`). `.board-grid:has(.shot-falling)` now raises the whole grid to `20` for the length of the drop; the layout test asserts it outranks all three.

## Hide and Seek: CPU Guests in Online Lobbies (2026-09-15)

**A host can fill the empty chairs with bots.** With only a few people online, a hide-and-seek round was thin — two players is one hider, and a demon reaching them ends it. The lobby now has a `CPU GUESTS` stepper under the guest list: the host asks for up to six, every client sees the same `CPU GUEST N` rows, and at start the server seats bots in whatever chairs are still empty. People always come first — a bot gives its chair to anyone who joins — and the seeker is always a real person; a bot only ever hides. It still takes two real people to start.

**A bot is a body with a bot at the keyboard, and nothing else.** The new pure `cpu-logic.js` (mirrored to `factory-network-server` beside `hider-logic.js`) reuses the solo stand-ins' brain and the map's navigator and turns a route into the *same input a human sends* — a facing, a forward key, sprint while fleeing, crouch once hidden, the flashlight on until tucked in, and a one-tick `interact` press on a shut door's handle. The server hands those inputs to the same `engine.tick` a client's reach, so a bot walks the same mover, spends a real battery, sprints on a real stamina meter, is tagged and is caught by demons under exactly the rules a person is under, and the authority has no channel through which to favour it. `cpu-N` ids are refused on the input message so a socket cannot drive one. Bots use the round's seeded random, so a match with bots in it still replays.

**`cpuCount` is a host-only lobby setting.** It lives in the server's `sanitizeLobbySettings` and `HOST_ONLY_SETTINGS`, because matchmaking compares settings and a searcher who never asked for bots must still join a host who did. The client never changes the count locally — it sends `update_lobby_settings` and draws what the lobby payload says. Proven headlessly on all four maps (bots reach a room, open a shut door, bolt from a staged seeker, get caught by roaming demons); not yet hunted by a person in a live round. 537 cabinet tests, 31 hide-and-seek server tests green. Both repos must deploy together.

## Speed Demon: Online Circuit Race Stops Rubber-Banding (2026-09-11)

**The server's circuit physics was a hand-written copy, and it had drifted.** Online circuit play was reported as "super buggy", and the root cause was that `factory-network-server`'s `circuit-race.mjs` was a re-implementation of the cabinet's `scripts/circuit/` rather than a mirror of it. It lacked the wall-separation nudge, so every barrier scrape put the server's car 0.75 world units from the client's prediction and the two never agreed again — 70–250px apart after twenty seconds of driving — and the client's reconcile snapped the car back on every corner. It also carried a roster of eight models while the cabinet ships twenty-four, so most cars were refused with "needs a Circuit Race car". The hand-written copy is gone: `tools/mirror-sim.mjs` now mirrors `scripts/circuit/` byte-for-byte into `shared/circuit/`, the server's only circuit code of its own is a PNG decoder that feeds the mirrored `createRoadMask`, and a new server test drives the mirrored round and a client-shaped race through twenty seconds of wall and car contact and asserts them bit-identical.

**Three timing bugs on top of that.** The authoritative sim advanced on the registry's generic 250ms heartbeat — four snapshots a second, each a quarter-second burst of physics, the opponent teleporting between them — so a circuit room now holds its own 20Hz timer, the hide-and-seek shape. The sim ran at real time, so every input arrived late by the network latency and was dropped for claiming a tick already passed; it now trails the clock by `CIRCUIT_INPUT_DELAY_MS` (150ms) so a tick sees the input made for it, and a late input is applied at the current tick rather than thrown away. And the round definition disagreed between the two sides: the client built its race with no countdown and the offline "local finish ends the race" rule, so both sides now build `countdownSeconds: 3` (the tree is simulated, tick 0 is `startAt` everywhere) and the new `finishRule: "all"`, with a server-side chequered flag thirty seconds after the first finisher so a parked driver cannot hold the round open to the five-minute timeout.

**A mirror manifest guards the copy.** The mirror tool writes `shared/sim-mirror-manifest.json` and the server's `mirror.test.mjs` checks every mirrored file against it, so a server copy edited by hand fails the suite — the thing that let this drift go unnoticed. Validated headlessly with two predicting clients at 60ms and 110ms latency (largest local correction 1.6px over ~300 reconciles); not yet played by two people in two browsers. 1419 cabinet tests and 94 server tests green.

## Puck'd Up: Several Designs, and the Death of a Dead Colour Picker (2026-09-10)

**A player now keeps up to eight designs and equips one.** The garage document grew a list: a **loadout** is a mallet and a table half with an id and a player-given name, and the **garage** is `{ version, equippedId, loadouts[] }`. There is always at least one — deleting the last one is not a state the document can be in — and everything downstream is still handed *one loadout*, never the list: the renderer, the stage preview, the rival kits and the public route an opponent reads. That is why the list cost those files nothing but a rename; `view.equipPlayer` takes the same shape it always did.

**Version-1 rows migrate rather than being wiped.** A document with no usable `loadouts` array is read AS one loadout, which is exactly what a row saved before this shipped looks like, and is the same path `{}` and `null` take to the factory loadout. Both normalizers do it, and `tests/server-agreement.test.js` now holds a real v1 document up beside twenty other malformed inputs and asserts the cabinet and the API still agree character for character — including on the new name rules (control characters out, whitespace collapsed, capped at 24, never blank), the eight-slot cap, duplicate-id renumbering, and an `equippedId` that names no slot falling back to the first.

**The slot being edited is the slot equipped, and that is the whole design.** Picking a slot equips it — in the Garage's slot bar and in match setup alike — so the panel can never be editing a design that is not the one on the table behind it, and there is no "apply to slot" step to get wrong. The Garage's bar is chips with two dabs of each design's own paint, an inline name field, and New / Duplicate / Delete, with the cap and the last-slot rule enforced by the document rather than only by the disabled attribute.

**The dead colour picker in match setup is now the loadout picker.** It had been dead since the Garage shipped: an equipped half owns its goal and trim, so `applyColors` skipped the player side entirely and the swatch changed nothing a player could see. Rather than delete the concept, the cabinet's player accent — goal burst, warm key light, online ready colour — now follows the **equipped mallet's primary colour**, and the space the swatches held is the list of saved designs plus a way into the Garage. Picking one there **saves immediately**, because an opponent's client draws the equipped slot off the public route: a choice that never left the tab would put a different mallet on their screen than on this one.

**The Garage returns to whoever opened it.** It is reachable from the main menu and now from match setup, and Back goes to the screen that asked — a player who stepped in from setup to build a second design has not finished choosing a rival and a venue.

**An opponent is shown one design and no more.** `puckdUpLoadoutFromGarage` resolves the equipped slot explicitly; the other designs and the names a player gave them never leave the account, and a test asserts the wire form contains neither. 194 cabinet tests and 804 API tests green.

## Puck'd Up: The Garage, and Two Halves of One Table (2026-09-10)

**A player's mallet and their half of the table are now data on their Factory account.** `Garage` is a main-menu destination in Puck'd Up: a parametric mallet builder (ten shape presets seeding eleven geometry parameters, nine material finishes over roughness/metalness/clearcoat, three colours, seven hardware styles built as real mesh, and the 40 approved decals on a thumbnail picker with position/scale/rotation/opacity) plus a half-table editor (twelve premium tabletop presets, a deterministic pattern generator, markings, rails, goal and side trim). Desktop is a right-hand panel over the live cabinet; phone is a bottom drawer with the scene rendered into the strip above it.

**It rides the generic `game_loadouts` row — the fourth cabinet to do so, and it needed no migration and no new route.** `GET`/`PUT /games/puckd-up/garage` with `services/puckd-up-loadout-catalog.mts` registered in `db/game-loadouts.mts` beside Speed Demon's, Yam Bowling's and Shark Hall's. Where Shark Hall's catalog validates id *namespaces* because its cosmetics are opaque ids, Puck'd Up's garage is mostly **numbers**, so this one clamps every one of them on the way in and on the way out, the way Speed Demon clamps a livery. `requiresEntitlements: false` is the switch for the day cosmetics are earned; nothing else in the stack changes.

**`localStorage` is not in the save path at all.** The editor's working copy is in memory, the account holds the truth, and `SAVED` is set only after a 200 whose returned document has *replaced* the working copy — so a server clamp the client did not apply still becomes what the player sees. A failure sets `SAVE FAILED` and keeps every edit; signed out, the Garage previews and says plainly that saving needs an account rather than inventing a persistent guest garage. This is deliberately stricter than Shark Hall's write-through cache: a design that survives only because one browser still has it is a design an opponent can never be shown.

**Two normalizers, one document, and a test that says they agree.** The cabinet normalizes so it never draws a document it would not have written; the API normalizes because it is the only one of the two that can be trusted. `games/puckd-up/tests/server-agreement.test.js` reaches across into `platform-api/` and asserts the default documents are byte-identical and that thirteen malformed inputs clamp the same way on both sides. The API cannot import the cabinet (it deploys alone) but a test in one checkout can hold both up next to each other.

**A cosmetic cannot reach physics, and that is asserted structurally rather than promised.** The mallet body stays `.73` in `physics/world.js` however wide a shape slider goes; `tests/cosmetics-isolation.test.js` checks the constant, checks that no cosmetic module names a gameplay quantity, and checks that no physics module imports one. The Garage ships a **collision overlay** that draws the true gameplay footprint over whatever design is equipped, so a player can see for themselves that a wide mallet and a narrow one have identical reach.

**The table is now two halves and a neutral centre.** The field is three planes and the side rails are cut into per-half sections plus a centre collar — a *visual* subdivision; `tableRails()` and every physics body are untouched. `applyHalfAppearance(side, half)` writes only the side it is handed, there is no side selector in the UI and no code path that would accept one, and the centre band, face-off ring, centre rail collars and cabinet body stay venue-themed so two strongly contrasting halves still read as one physical table. In a CPU match the far half is the **rival's own kit** — each of the twelve rivals mapped to real catalog items and drawn through the same renderer — rather than a default.

**Garage preview and match gameplay are the same renderer because they are the same scene.** The panel is an overlay on the cabinet's own canvas, so Match View is literally the match camera and there is no editor-only path to diverge. `render/mallet-appearance.js` rebuilds geometry only when a number that builds geometry changes, and `render/table-surface.js` repaints a half's canvas texture only when a surface parameter moves — never per frame — disposing what it replaces.

**The prototype's full-field designs are not ported and are not coming back.** Street Paint and Black Ice are gone; the twelve replacements are premium manufactured surfaces built on a readability rule with teeth: every pattern declares the zones it may draw in, and one that is not allowed in the `field` zone must either clip itself to the perimeter and rear apron or draw with strokes only. `tests/table-surface.test.js` enforces that, and also that painting is deterministic — the same surface draws the same pixels twice, which is what lets two clients render the same player's half the same way.

**A real bug was found and fixed in shared platform code.** `createPlatformApiClient()` never exposed `get`/`put`, yet **Speed Demon's and Shark Hall's garage stores both called them** — so every real save had been throwing a `TypeError` into a `catch` and falling back to the local cache, invisibly, because both cabinets' tests inject a fake client. The shared client now exposes the raw verbs plus named `fetchGameGarage`/`saveGameGarage`/`fetchGamePublicLoadout`/`fetchGamePublicLoadouts` helpers, which fixes those two cabinets as a side effect. `games/puckd-up/tests/garage-roundtrip.test.js` exists so it cannot happen again: it stands the real API app on a real HTTP port and drives it with the real cabinet store through the real shared client, with only the Postgres row substituted.

**Not done:** custom decal *upload*. The document, the server validation and the renderer all support a custom decal end to end as a controlled `{ assetId, https url }` reference — a data URI or a half-valid reference degrades to `none` — but there is no `POST /upload/puckd-up-decal` endpoint and no UI to mint one yet. Online opponent halves are likewise ready but unwired: the public loadout route and `view.equipOpponent` are in place, and what is missing is the lobby protocol carrying each seat's platform `playerId`, which today only carries a display name.

## Shark Hall: My Table, and a Cosmetic Foundation That Cannot Touch the Sim (2026-09-03)

**A player's table is now data they own, kept by the Factory.** `My Table` is a first-class main-menu destination — not a row inside Settings — opening a full-screen 3D editor over the live table: orbit with a drag, zoom with the wheel or a pinch, a category rail, an item tray, and honest `Equipped` / `Previewing` states. 123 catalog items across nine cloths, seven grain-styled timbers on rails and apron independently, hardware, cushions, pocket liners, sights, apron decals, six ball sets, six table presets — plus a whole hall layer (walls, floors, fixtures, art, cue racks, shelves, signs, furniture, rugs, windows, four room presets) and four award types. **Everything is unlocked in this phase**, through one visible line rather than by omitting locked support.

**The load-bearing decision is that `scripts/cosmetics/` is a SECOND pure layer beside `sim/`** — no THREE, no DOM, no clock, no random, no storage, no network, and no imports from outside itself, including from `sim/`, because a cosmetic has no business reading a ball radius. `tests/modules.test.js` enforces it, which is why the entire catalog is validated in CI under node rather than by opening a browser. The renderer is handed **resolved presentation payloads, never item ids**: `render/` cannot look a cosmetic up, cannot ask whether it is owned, and cannot reach the catalog, so ownership and slot compatibility are decided in exactly one place.

**"A cosmetic is presentation only" is asserted structurally, not promised.** Every payload is walked recursively for `radius`, `mass`, `friction`, `restitution` and their relatives, and `tests/table-geometry.test.js` builds the table twice under two wildly different loadouts and asserts identical geometry down to the cushion faces. A cushion cosmetic is a colour; how a cushion plays is one number in `sim/physics.js` and is the same on every table in the game. Only two things rebuild geometry at all — the rail sights and the apron decal, whose *shape* is part of the choice — and neither is collided with.

**Ownership is a one-line seam with the locked path already written.** `DEVELOPMENT_INVENTORY = createInventory({ grantAll: true })` is the development grant, spelled out where anyone can find it; underneath it `isOwned` can return false, `normalizeLoadout` already refuses an unowned id, and the tray already draws a locked chip. Turning progression on is therefore a configuration change rather than a rewrite — which is exactly what faking the grant by leaving locked state out would have cost. The four award types (`trophy`, `plaque`, `framed-award`, `championship-display`) are deliberately separate types with non-development sources, so a tournament trophy is never equippable merely because it exists in the catalog.

**Storage is the Factory, not the browser, and it needed no new table or route.** `store/cosmetics-store.js` reads and writes `GET`/`PUT /games/shark-hall/garage` — the same generic `game_loadouts` row Speed Demon's garage and Yam Bowling's presentation loadout already use — with `localStorage` as a write-through cache keyed by player id and a frame-loop retry backoff behind a failed push. The new server piece is `services/shark-hall-loadout-catalog.mts`, registered in `db/game-loadouts.mts`, and **it validates document shape and per-slot id NAMESPACE rather than mirroring the cabinet's item list**: bounded entries and strings, and every id in the prefix its slot requires, so a cloth id cannot be stored in the ball-set slot. Mirroring the roster would have been a second copy to drift for a payload with no exploit in it. This is the reusable half — a fourth cabinet wanting server-backed cosmetics registers a catalog and writes no route code. When cosmetics become earned, `requiresEntitlements: true` plus an owned-id check in `slotId` is the whole change.

**Two content rules have teeth rather than good intentions.** A ball set declares a `readability` block and `readabilityOf` computes the same block from its actual colours; the test asserts the two agree, and it caught a real collision between Brass Hall's 3 and 7 during authoring — a set that is attractive and ambiguous fails the suite. And a **finish is not a hex code**: every timber carries a `grainStyle` (figured, ribbon, straight, brushed, lacquer) with its own stroke count and amplitude that `render/textures.js` draws differently, floors branch on `pattern`, cloths on `weave`. The cloth catalog also carries a luminance floor, encoding the bug this cabinet already shipped once — below roughly 0.10 the pendant's sheen outweighs the cloth's own colour and the table renders grey slate.

**Nothing is persisted by a click.** The editor holds three states — saved, working, dirty — and previewing writes the working loadout and reaches no store at all. Back and switching saved tables raise the same save-or-discard question through the same prompt. `ui/table-editor.js` is a view: its API is `load`/`preview`/`applyPreset`/`save`/`reset`/`discard`, so the UI never learns where a table is stored. The editor is a **layer rather than a menu panel**, and one `body.editing` class puts the whole playing cabinet away and promotes the canvas from its bordered 1240px stage to the full viewport — necessary because the arcade link sits exactly where the editor's own Back does. Desktop and phone are two layouts over one tray, in CSS, with a test asserting the tray is not forked.

**Verified in a browser on desktop** — the editor opens over the live table, presets and every category repaint it, the brass nameplate decal lands on the apron, orbit and zoom work, save/dirty/discard behave, and the cabinet plays normally after leaving. 278 cabinet tests and 779 API tests green. **Not verified**: the phone layout on a real device (its rules were checked by applying them to a desktop viewport), and offline play still has no full human playtest to the 8.

## Hide and Seek: The Hotel Becomes Shared, and Reaches the Grid (2026-08-29)

**Every remaining online gap had the same shape: state living in the renderer, where only a browser could answer for it.** An online round used to be seeker-versus-hiders in an empty hotel where a door you opened was still a wall for the person chasing you. It is now the whole game.

**`fixtures-logic.js` owns the building's moving parts** — every room door, secret panel, drawer, key ring and the elevator — as pure contested state. A door you open is open for the seeker; a drawer holds one key and whoever searches it second finds it empty; the cabin carries whoever is standing in it. The piece that made this portable is the **targeting**: a client aims with a `THREE.Raycaster` against meshes, and the authority aims with distance, then height, then a facing dot against plain records, because a server has no meshes to raycast. So a client sends "I pressed E", never "I opened door-201". Interaction is **edge-triggered on the authority**, which is also why the input contract had to start sending the key *release* — an `interact` flag that latches true means the next press is not an edge at all.

**`demon-logic.js` is the demon's brain, lifted whole out of `modules/monster.js`.** `enemy-logic.js` already held the rules; what was still trapped beside the shroud rags and the fresnel shader was the thing that used them — where a demon stands, which waypoint it walks, when it replans, who it catches. The rendering module keeps a `setRemotePose` puppet mode: online the body is still drawn and animated, but it detects nothing, routes nothing and catches nobody. There is one authority per hotel. This is the reusable half of the change — the same extraction is what any cabinet with a rendering-coupled AI will need before it can go server-authoritative.

**A demon's catch now requires line of sight**, the same as a seeker's tag. It was distance-only, which quietly made a shut door real cover against a player and no cover at all against a demon. The ray only runs for a candidate already within arm's reach, so it costs nothing on an ordinary tick.

**Identity, the sign-in gate and reconnection** arrived through the shared seams. `modules/account-access.js` reads the factory profile and gates online entry with `factory-account-gate.mjs`, and never writes back — Factory-Identity-First, so the alias over a body in a corridor is derived and the platform profile is not this cabinet's to change. A dropped guest keeps their body: it stays standing and catchable for a 30-second grace window while they walk back into it, which is a free find and the honest consequence.

**One repo-wide trap surfaced and is worth knowing before the next lobby game.** `find_lobby` must send the game's own seat limits. A search that omits them is sanitized to the server-wide default of 2-6, which never equals a lobby created with different limits — so every guest silently opens a room of their own and nobody can see anybody. The symptom looks like a broken socket and is not one.

**The cabinet is now on the grid as number 16**, with a `game.json`, an `arcade-catalog.mts` entry and the preview that was already sitting in `grid-previews/`. It is served **from the repo root** rather than the cabinet folder, because the sign-in gate imports `/js/platform/**` — `server.mjs` roots at the repo and opens `/games/hide-and-seek/`, so the one-click local launcher matches Pages. `main.js` stayed inside its 160-line architecture cap by shedding two things that were never composition: `window.HotelPrototype` moved to `modules/prototype-api.js`, and the local player's camera-to-avatar sync moved into `modules/avatars.js` beside the pose a network snapshot drives.

**Not done, and the reason it is not:** nobody has played an online round with real people. Connection, lobby, role assignment, the head start, the shared hotel and both demons were all driven in a browser against a live local server, but Chrome freezes `requestAnimationFrame` in unfocused tabs, so two scripted clients cannot both simulate — how movement *feels* between two machines is exactly what automation cannot report. A two-player round is also fragile by design: with one hider, a demon reaching them ends it, which in a scripted run happened fourteen seconds in while the seeker was still shut in the cabin. Three or more guests is where the game lives.

## Mini Hoops: HORSE Keeps the Setter Honest, and Goes Online (2026-08-26)

**The turn rule changed, and it changed the shape of the mode.** Matching a standing shot used to pass control to whoever matched it. It no longer does: **the setter keeps setting until they miss a shot of their own.** Answering a shot is staying alive, not taking the initiative — the previous rule made a made match the *reward*, which reads backwards. The turn now changes hands in exactly one place, a setter missing their own setup, and that player still takes no letter for it, which is what keeps them from farming letters with impossible shots.

**Online HORSE shipped, server-authoritative rather than relayed.** Its own game id, `mini-hoops-horse`, so a player looking to spell a word never lands in the timed-score pool; reached from the Factory Network screen's Game select, which now routes two modes rather than one. `factory-network-server/games/mini-hoops/` grew a second definition off the same mirrored sim — `horse-adjudicator.mjs`, `horse-match-engine.mjs`, `horse-lobby-game.mjs` — and the mirror now carries `bin-physics`, `bin-placement`, `horse-shot` and `horse.js` as well.

**A placement is authoritative mid-match state, and that is the new shape.** Every other server-owned game in this repo settles its config in the lobby and then rules on inputs; HORSE has a player *building the target* on their turn. The setter submits the bin, the server re-clamps it through the cabinet's own `normalizeBinSetup`, and the opponent draws the server's copy — otherwise an invented bin hands the other player a shot the rules never allow, and the two courts disagree about where it even is.

**`aimY` does not travel, and the release moment does.** The classic cabinet's reticle rides one fixed line and the server pins it; HORSE's vertical aim IS the placed bin's rest height, which the server already holds — so the pin the old handoff note called for turned out not to be needed at all. What the client does choose is the phase of the bin's motion clock at release, and that is not a hole: a player may watch a moving bin for as long as they like, so every phase is legitimately reachable and picking one is the skill the motions exist to ask for.

**The opponent's shot is replayed on your court rather than reported to it.** Their pull, their bin and their release moment played back through the same sim that ruled on it, so a letter arrives with a ball attached.

The lobby markup's classes were renamed `ttt-online-*` → `mode-lobby-*`: two modes now hang the same four-question lobby off the same screen, and the shape should not be named after whichever needed one first. Not built: HORSE reconnection — the server pauses and forfeits on a grace period, but the client has no `resumeSavedSession`. Not yet played by two real browsers on the live server.

## Yam Bowling: Binding the Mastery Ladder (2026-08-16)

The audit below found the bowler mastery ladder was **20 of 31 rewards label-only** — a name and nothing wearable — while the scope doc marked Milestone 5 complete. Five of those are now bound, which is every node that needed no new art.

**The two mastery titles are the first reward whose id is scoped to the bowler who earned it while the slot it fills is global.** You are Reina's master whichever bowler you then take to the lane. That one fact drives the whole shape: the cadence declares a `bowlerTitle` slot that `resolveEquipment` maps back onto `title`, so the equipped check stays a plain global-slot comparison; the server registers `title:<bowler>:nameplate` and `title:<bowler>:master` *without* a `characterSlug`, because a global slot matches on type alone and a character-scoped registration would never validate; and their grant is the only one that has to know which track crossed the level, since the entitlement id carries a `{track}` the award resolves. Migration `042` backfills them per track rather than per player, unlike every other level reward. They are text-only on purpose — a ladder that can only pay out when new art exists is a ladder that stays unbound.

**Every strike burst now has an earn route.** Four claimed `bowler-level` while no node paid them; `red-supernova`, `sky-shatter` and `diamond-spark` bound to mastery levels 8, 22 and 23, and `lime-pop` moved to the *player* ladder's level 30 — the mastery ladder has no lime trail to pair it with, and that summit had nothing equippable on it either. A pre-existing test asserting every burst has an earn route is what rejected the first attempt, which had tried to restate the two leftovers as "acquisition deferred" instead of binding them; the repo had already decided that question and the test held the line.

Migration `042` rather than an edit to `041`: a shipped migration is never edited, so the coverage test now reads both as one corpus and a node bound after `042` will need `043`.

**The remaining 15 nodes cannot be bound without new content.** Ten need art — three skins, two victory poses, four player cards, one menu splash — and binding them to existing art would either re-gift founding content or hand out part of a skin entitlement, which the product rules forbid. Five need a whole new reward type that exists in neither the cabinet catalog nor the server's slot map: profile icon, character banner, two entrance stingers, lane emote.

## Yam Bowling: a Metagame Audit, and Level Rewards That Actually Save (2026-08-16)

An audit of `games/yam-bowling/METAGAME_SCOPE.md` against the code, then the fix for the one live defect it turned up.

**A level reward could be equipped but never saved.** Milestone 8 established that "a level reward needs no entitlement row" — the client derives ownership from the synced level, so minting a durable grant for "reached level 13" would duplicate a fact the account already owns. That reasoning is sound, and it was only ever taught to the *client*. `db/game-loadouts.mts` builds its ownership context from `game_entitlements` alone, so `normalizeYamBowlingGarage` dropped every level-earned slot value it was handed, and because a save reapplies the server's sanitized answer, the player watched a newly earned trail, burst or badge revert the instant they saved it. Every player-ladder trail and burst was affected, plus the three bound mastery badges. All suites were green through this: each side was tested against its own assumption and nothing covered the round trip.

**The fix was already in the file, one field away.** Skin Vouchers at player levels 10 and 25 are level-triggered rewards the *server* mints: `playerInventoryRewards` declares them and `awardMatchXp` grants them inside the XP transaction by comparing previous and next XP. A level-earned cosmetic is that same shape with a different destination, so it is now `levelEntitlements` in the same definition, granted by the same transaction — `game_entitlements` instead of `game_inventory_items`, with a matching per-track pass for bowler mastery. The garage validator needed no change at all: the row exists before the cabinet can equip the item. This retires the "no entitlement row" rule, which was the outlier — every other durable reward in the repo is a row — and demotes the client's `applyLevelUnlocks()` to an optimistic mirror.

Both ladders' bound rewards turned out to be **global** cosmetics, which is why the mastery grant is keyed to the player rather than the track: reaching level 13 with any bowler earns that badge once. The cadences now have a server-side twin, which is the house style rather than a compromise — `tactical-arena-reward-catalog.mts` opens by telling you to keep it in lockstep with the client's `unlocks.js` — and a test asserts every level-granted id survives `normalizeYamBowlingGarage`, so the two registries cannot drift apart silently. Migration `041` backfills accounts that crossed a node before the grant existed, the job `040` did for vouchers; its thresholds are literals so it keeps meaning what it meant after a curve is retuned.

Three mastery badges (`laser-focus`, `precision-bowler`, `lane-legend`) were bound in the ladder but absent from `yam-bowling-loadout-catalog.mts` entirely, so they would have been stripped as unknown ids even once the level check existed. Now registered.

**What else the audit found, recorded in the scope doc rather than fixed.** The bowler mastery ladder is **20 of 31 rewards label-only** — including both level-30 nodes, the promised summit of the track — while the doc marked Milestone 5 complete and admitted only the player ladder's 8 pending nodes. Four strike bursts carry `unlock.source: "bowler-level"` while no mastery node pays them. And the three *match* achievements (`perfect-game`, `comeback-kid`, `split-decision`) have been shipped end to end for some time without the scope doc mentioning them, which is what made the checkbox state untrustworthy as a work queue in the first place; Milestone 7's achievement list is now split into match achievements (shipped) and threshold achievements (unbuilt, and needing a claim prerequisite the server checks against its own `game_xp_*` rows).

Not browser-verified: that still needs `factory-network-server`, the API and a signed-in client running together.

## Yam Bowling Progression: the Domain and the Third Persistence Shape (2026-08-15)

Milestone 4 of Yam Bowling's metagame plan — XP and mastery levels that are worth trusting. Two slices: the cabinet's progression domain, then the platform tables that make it authoritative.

**The client never awards itself XP, and that is a structural fact rather than a promise.** `games/yam-bowling/progression-core.js` owns the curves, the grant table, eligibility, forfeits, and a device-local cache. `computeMatchGrant` says what a finished match is *worth*; `applySnapshot` — the server's answer — is the only path in the module that moves a balance, and `project-structure.test.js` asserts there is exactly one such write. A grant the server has not answered sits in a pending queue that survives a reload, so a result bowled with a flaky connection is neither lost nor spent, and the results screen will be able to say "syncing" without inventing a total. A refused grant joins the ledger instead of being retried forever.

**A level is derived from XP and never stored, anywhere.** A stored level disagrees with the curve the moment the curve is retuned, and nothing afterwards can tell which of the two is wrong. Both curves cap at 30; the player curve is dearer than the bowler curve so that spreading play across the roster does not outrank mastery of one.

**Migration 038 is the third table shape generic on `game_slug`,** after loadouts (035) and run records (036). `game_xp_profiles`, `game_xp_tracks`, `game_xp_grants` — and the column is `track_id`, not `character_id`, because a track is a bowler here and a car or a unit elsewhere. It is deliberately not folded into 019's `game_progress_*`: that family owns *entitlements*, which are granted and then spent, while this one owns *earned advancement*, which only accumulates, and one table doing both would need every reader to know which of its rows meant which. Onboarding a cabinet is one entry in `services/progression-catalog.mts` — the third of the per-game catalog seams, beside `ladder-catalog` and `leaderboard-catalog`.

**The award rides the ELO transaction, on the same session id.** `game_rating_sessions` already decides "this match settles once", so reusing its key means no second call for a network drop to lose and no second key free to disagree. But the two halves are asymmetric in a way that took some care. An ELO update grades *both* players in one transaction, so only the first reporter runs it. XP is earned individually, from an individual bowler, so `game_xp_grants` is keyed by `(player, game, grant)` and the second reporter is still paid — which meant the already-processed branch had to stop rolling back and start committing.

**A disputed mode is clamped, not refused.** The first reporter stamps `mode_id` on the rating session; when a later reporter names a different mode, both are paid the *lesser* of the two payouts. The obvious design — refuse the mismatched claim — would have handed a griefer a way to deny an honest opponent's XP by reporting an inflated mode first. Clamping pays the honest player for what they actually played and earns the liar nothing, so one honest participant makes mode inflation pointless.

The residual trust is written into the migration rather than left implicit: a reporter still self-reports that a match happened at all, exactly as the ELO report has always done. `track_id` is unchecked but low-stakes (it picks who receives the XP, not how much), and the performance count is unchecked but hard-capped. Closing the rest needs `factory-network-server` to attest results over a shared secret — the same open question Speed Demon's ranked survey reached — and this schema would not change for it, since the attester writes these same rows under these same grant ids.

Reads are public (`GET /progression/:gameSlug/:playerId`), for the reason a driver profile is: a mastery level exists to be shown, and milestone 6 renders an opponent's on a Match Found card. There is no write route at all.

The account-deletion guard caught the new tables before they shipped, which is what it is for. The grant ledger goes with the totals rather than being kept as history — its only job is to stop one match paying twice, and a re-registered account starting from zero has no match left to double-pay.

The economy table is a twin: the same numbers live in the cabinet (so it can describe a pending grant offline) and in the catalog (which is authoritative). They are deliberately not imported across the project boundary, so both suites assert the table explicitly and a half-applied retune fails loudly in the file that was not edited.

**The cabinet now reports.** `online/progression-reporter.mjs` builds the block a finished online match carries and opens no connection of its own — it rides the existing rating report, so one request, one session id, one thing for a dropped connection to lose. Strikes are counted off the scorecard (a roll of ten can only happen off a full rack, so counting tens counts strikes exactly, tenth-frame bonus balls included), and a disconnect forfeit is read from the server's own `result.reason` rather than guessed: the leaver earns nothing, the player left standing files a forfeit rather than a win.

The pending state is honest by construction. Preparing a report queues the grant *before* the call, and only an accepted response settles it — a network failure is not a ruling. Until then the results screen says the XP has not synced instead of showing a total the server never agreed to. A lost *response* self-heals, because the next sync finds the grant in the server's own list and clears it; a request that never landed is still outstanding, which is the one gap left.

**Player rooms, the first cosmetic that ships locked.** `room-core.js` catalogues the thirteen backdrops of a bowler's own space, and it is the proof the milestone-2 contract works: a `room` reward type, a global loadout slot, and an ownership gate, with no change to any existing item, slot, or call site. Only the starter room is `founding`; the other twelve are `campaign` or `achievement`.

That split is only honest because rooms are *new*. Retro-fitting a lock onto content players already chose takes something away from them, which is why the nine lanes stay unlocked and why the same question had blocked them — a brand-new cosmetic has no such debt. Rooms also own no persistence and carry no legacy key: lanes and menu splashes each keep one because they predate the loadout, but the loadout has been the room's only owner from the first line. `asset-budget.test.js` now enumerates rooms too (799 -> 812 images, +1.96 MB against a 56 MB budget), so a half-processed room cannot ship.

**The circuit's first slice.** `campaign-core.js` and `ui/circuit-screen.mjs` add five divisions — Local Alley, City League, Regional Series, National Tour, Yam Championship — where each sanctioned match is a named rival at a named house, and beating one adds that bowler to the playable roster. Five starters, an achievement per match, and a promotion match closing each division at regulation length.

**Unlocks now have two owners, deliberately.** The campaign owns which *bowlers* are earned; the loadout owns every *cosmetic* owned and worn. The seam is asserted in both directions — `campaign-ui.test.js` forbids campaign-core from naming a skin, room, trail or burst, and `project-structure.test.js` forbids rooms from reaching for campaign-core. The consequence to keep in view: circuit unlocks live in device-local storage rather than in a server-backed grant ledger, so a bowler earned on one device is not yet earned on another. Making that authoritative is the same work as making cosmetics authoritative, and both wait on turning `validateTacticalArenaPublicClaim` into a per-game entitlement validator.

656 API tests (+39), 278 cabinet tests (+85), 73 shared-frontend. Not browser-verified end to end — that needs `factory-network-server`, the API, and two signed-in clients running together.

## Speed Demon Personal Bests + the Platform's Second Rating Source (2026-08-08)

Speed Demon's results screen used to show you a time and nothing to beat. It now keeps personal bests, submits them to a global board, and places one of them on the cross-game ladder rail — which meant teaching the platform that a ladder can be built on something other than head-to-head ELO.

**A board is a mode and an objective, and both exclusions were measured rather than assumed.** Seven boards: four distances, three clocks. Not per track, because `ui/track-layout.js` keeps the painted road geometry in one shared `ROAD` and only the dash rhythm and scenery differ — the five tracks are the same road in five settings, so splitting them would be five boards competing for one time. Not per car, because the roster is cosmetic-only and every model performs identically, so per-car boards would be 24 copies of one ladder. Both are still recorded per row as display metadata. If stat differences ever land, the board id gains a class segment and every existing id keeps meaning what it meant.

**The second rating source.** `services/ladder-catalog.mts` had anticipated this in a comment since it was written — "when a second source appears, add its `source` value here and give `db/ladders.mts` a branch for it — do not fork the registry" — and that is exactly what happened. `game_run_records` (migration 036) sits alongside `game_ratings` (018) rather than inside it: an ELO row is a running total that moves both ways, a record row is a high-water mark that only improves, and one table holding both would mean one of them lying about what its number means. The two sources are merged in JS rather than UNIONed, because a union would reconcile two orderings and two meanings of "rating" inside one statement to produce a ranking that is re-sorted afterwards anyway.

Only the quarter mile is registered on the rail. Registering all seven would bury every other cabinet's ladder under one game; the full set is served by `/leaderboards/speed-demon`, which the cabinet reads directly. A run-records placement is shaped identically to an ELO one — W/L/D zeroed, since a solo board has no opponent — so the profile rail renders it without knowing which it got, and `formatLadderRating` delegates to the board because a quarter mile best is `11924` in the column and `11.924s` on screen.

**Bests work signed out, and that is the deliberate opposite of the garage.** A garage requires an account because a livery only means something if an opponent can see it — a paint living in one browser cannot be shown to anybody. A lap time is the reverse: meaningful to the player alone. So signed out the cabinet keeps bests locally and says `SIGN IN TO RANK IT` rather than pretending to be offline, and signing in adds the board on top. The consequence is that the local copy is not merely a cache: `load` merges **per board** rather than letting either side win, since the server can be behind on a run set offline and the local copy can be behind on one set from another machine. Signed-out bests live under a fixed key and are never donated to whoever signs in next — two people sharing a machine is indistinguishable from one person signing in on their own laptop.

**A submitted time is a claim, and it is stored as one.** This is the part that matters for a *global* board, where the alternative is believing whatever a console call reports. Three steps, and only the third is a guarantee: the catalog refuses values outside the board's plausibility bounds (the impossible, not the suspicious — a 3-second quarter mile, not a fast one); the run is stored `verified: false` with its input log alongside; and a later pass replays that log through the deterministic sim to settle the flag. **The third step is deliberately not built.** Keeping the log now is what makes it possible at all — retrofitting verification later would mean discarding every record already set. An implausible value is refused rather than clamped, because clamping would *store* the forgery.

Speed Demon was unusually well placed for this: `sim/input-log.js` and a deterministic sim already existed, because `factory-network-server` replays both drivers' logs to adjudicate online races. The gap was that the log was recorded for online runs only, so `recordThrottle` moved out of `tickOnline` to the one point both paths reach on the way to `stepRace`, with `raceTick` incrementing beside it — that pairing (exactly one tick per `stepRace` call) is the invariant `replayRun` mirrors.

**Ranked 1v1 is prepared, not built,** and the survey is written down in the cabinet's `CLAUDE.md` so it does not have to be redone. `ranked_queue`/`ranked_matches` are already keyed on `game_slug` and `db/ranked-elo.mts` is pure and names no game, so the whole brokering stack is reusable; the only accidental coupling is two Tactical Arena-shaped columns (`board`, `ban_first`) that a second game has to make nullable first. The rating slug is `speed-demon-ranked`, following sumorai, which keeps casual play off the ladder — and it will have to join the list `POST /ratings/:slug` refuses by name, or a client could invent a session against the public ladder. The open question is adjudication: `factory-network-server` already knows who won but has no database and no way to tell `platform-api`, and there is no service-to-service auth on `platform-api` today. Either two-sided client attestation (the Tactical Arena pattern, no new trust boundary) or a shared secret (stronger, but a second authentication surface). Not decided.

Three live bugs fell out of the client tests, all one root cause: `Number(null)` is 0, and **0 is a legal value on a time-attack board** — a run that never moved covered no distance. So an unset field coerced through `Number` read as a real, achievable result. It gave a record to a race that never finished, kept malformed saved rows alive through normalization, and printed "no best yet" as `0.000s`.

597 API tests (+22), 1063 cabinet tests (+38). Browser-verified end to end: a run files, a faster run reports its delta, an equal run does not, and a guided practice run records nothing — the coach stops the world, so its clock bears no relation to a driven one.

## Founder Friendship + Announcement Broadcast (2026-08-04)

Two "the platform greets you" features, both of which are really about one thing: a new account should not land on an empty arcade.

**The founder friendship.** Every new sign-up is befriended with the account behind `FOUNDER_EMAIL` — the Tom pattern. It is a real friendship through `createFriendshipBetweenPlayers`, not a special-cased row: symmetric, ledgered, worth the standard +100 friend points, syncing the `friends` column and `player_metrics` on both sides, and removable by either party like any other. A second concept of "friend" that only this feature understood would have to be taught to every surface that reads the friends rail.

Two properties are deliberate and worth not undoing. It is **opt-in by configuration** — `FOUNDER_EMAIL` is its own variable, never derived from `ADMIN_EMAILS`, because granting someone operator authority must not also make them everyone's first friend; unset means the feature does nothing and does not even spend a database round trip finding that out. And it is **best-effort** — a missing founder account, a database hiccup, anything, and registration still succeeds. Someone who just handed over an email and a password has to end up with an account; a cosmetic friendship is not worth failing that over. `services/founder.mts` returns a reason string for every failure mode and throws for none of them.

It is awaited rather than fired and forgotten: the client goes straight to the profile after registering, and a friendship landing a second later would show an empty rail on the very first page load — the one load this feature exists to fix.

**Announcement broadcast.** Publishing a bulletin or adding an event now notifies every registered account — `bulletin_posted` (summary as the preview, link to the board) and `event_posted` (deep link to `/event/?slug=`, since events have their own page). These are the only two writes in the platform addressed to everyone at once, so they share one implementation, `services/content-announce.mts`, differing only in a small per-kind config.

The once-only guarantee lives in SQL rather than application logic. Migrations `033`/`034` add `announced_at` to both tables, and the claim is a conditional `update ... where announced_at is null and <visible> returning id`. Two concurrent publishes cannot both win — Postgres serializes the update and the loser matches no rows. An operator fixing a typo on live content re-notifies nobody, because the claim has been spent.

The fan-out is one statement — `insert into notifications ... select from accounts` — with deterministic per-row ids (`notif-<kind>-<id>-<playerId>`) and `on conflict (id) do nothing`, so a retried broadcast lands on the same rows instead of giving everyone a second copy. The kind is *in* the prefix because both id spaces are uuids from the same generator; without it, a bulletin and an event that happened to share an id would silently swallow each other's announcement. The publishing operator is excluded from their own blast.

**What counts as visible differs, and that's the interesting part.** A bulletin has a draft state, so `published` + `public` is the gate and the operator has a natural staging area. An event has no draft — the calendar hides only `cancelled` — so a `scheduled` event is public the instant it is created, and *creating* it is what notifies. The gate is `scheduled` or `live`: `completed` is excluded because announcing a past event tells everyone about something they can no longer attend, and that exclusion makes `cancelled` usable as the staging state, since it never spends the claim. Create quietly as cancelled, flip to scheduled, and the flip is what goes out. This is documented in the admin guide, because it is the one place the two flows genuinely behave differently.

Announcing is a side effect of the save, never a precondition for it: the service swallows its own failures, the route wraps the call anyway, and the audit entry records `notified` and `recipientCount` so the Audit tab can answer "did that actually go out?" after the fact. A `friends` or `private` bulletin never broadcasts — the fan-out has no audience filter, and pretending otherwise would leak content past its own audience. Deleting either kind clears its notifications through `deleteNotificationsByPayloadRef` (`contentId` added to the allowlist), so no alert outlives what it points at.

Both headlines lead with the announcement (*📢 New bulletin: Tournament Night*, *🗓️ New event: Sactown Smackdown X*) rather than with the poster. Every account receives them; "Jay posted a bulletin" reads as a social action when it is a noticeboard notice.

## Bulletin Attachments (2026-08-04)

Bulletins can carry one image — the immediate need was a tournament flyer for Sactown Smackdown X. Migration `032-bulletin-image.sql` adds `bulletins.image_url`, mirroring `012-thought-image.sql`: a plain URL column with an empty-string default, not a join to `player_photos`, because a bulletin image belongs to the announcement rather than to anyone's gallery.

Upload reuses the existing `/upload/photo` endpoint rather than adding an admin-only one — it already validates by magic bytes instead of the client's declared MIME type, and caps the long edge at 1200px with `crop: "limit"`, so a portrait poster is scaled whole instead of cropped.

**Posters letterbox, they don't crop.** Both the board card and the console preview constrain the image by max-width *and* max-height and let the frame size to its content, so a 2:3 flyer isn't stranded in a wide black band and a landscape banner still fills the card. Cropping a flyer to a landscape strip would cut the date, venue, and entry fees off the bottom — the exact information the announcement exists to carry. Same no-destructive-crop rule the profile backgrounds follow.

`httpUrl` in `db/content-shared.mts` restricts the column to http(s), with the same guard repeated in the browser normalizer — the value lands in an `<img src>` on a public page, and the column is writable through the admin API.

One non-obvious fix: the console re-renders the whole panel when an upload completes, which would have silently wiped a half-typed announcement. `state.bulletinDraft` captures the form before the re-render. Edit-vs-create mode still reads the *stored record*, not the merged draft — a draft on an unsaved bulletin is truthy but has no id, which would otherwise offer Delete on nothing.

## Admin Console (2026-08-03)

The platform gained an operator. Until now every route in `platform-api` authorized the same way — *is the caller the owner of this row?* — which is right for player data and cannot express "the person who runs the arcade". Bulletins and events were frozen arrays compiled into the browser bundle (`DEFAULT_BULLETINS`, `DEFAULT_EVENTS`), so publishing an announcement meant editing TypeScript and redeploying. There was no moderation beyond self-deletion of your own comments.

**Authority is a column, not a token.** Migration `031-admin-console.sql` adds `accounts.is_admin`, and `db/admin.mts#isAdminPlayer` is re-read on *every* admin request rather than trusted from a JWT claim — tokens live 30 days, and a revoked admin has to lose access on their next call. `ADMIN_EMAILS` on Railway promotes matching accounts at boot; it can grant but never revoke, so a typo in an env var cannot strip authority from a live operator. `/auth/me` reports `isAdmin` purely so the nav can show an Admin link; nothing server-side trusts it. Demoting the last admin is refused inside a transaction, because there would be no way to grant the flag back.

**One gate, one place.** `routes/admin-routes.mts` matches the whole `/admin/` prefix, checks authentication then authority, and only then delegates to `admin-content-routes.mts` and `admin-moderation-routes.mts` — neither of which contains any authorization logic. A new admin endpoint added to either is gated by construction. Every state-changing action writes to `admin_audit_log`; the audit-coverage test walks all twelve mutating routes and asserts each one logs exactly one attributable entry.

**Admin deletes are separate functions, not a flag.** `db/thoughts.mts` and `db/photos.mts` bind ownership into the delete predicate itself. Threading an `asAdmin` escape hatch through them would put the ordinary player's ownership guarantee one mistaken argument away from being skipped, so `db/moderation.mts` holds ownership-free deletes reachable only from the gated route. The player-facing paths were not touched.

**The stability contract for cabinets.** `cabinet_overrides` rows only change how the *grid presents* a cabinet — `games/<slug>/game.json` stays the authority for what a game is. `applyCabinetOverrides` in `arcade-catalog.mts` is the only place admin data touches the grid, and it will not change `href`, `slug`, or `previewImage`, will not add a cabinet the catalog does not already have, and treats every null field as "inherit". Deleting a row restores the shipped presentation exactly. `js/tests/admin-catalog-overrides.test.mjs` pins all of it, including that a hostile row cannot repoint a card.

**Null vs empty is load-bearing.** `content-api.mts` returns `null` when the platform could not be asked and an array when it answered. Only `null` falls back to the shipped fixtures; `[]` renders an empty board. Without that distinction an operator who unpublishes everything would watch demo content reappear with no way to clear it. A backend outage degrades the public pages to exactly the pre-console site.

**Suspensions are time-boxed.** A suspended account keeps read access — which is what makes the notice visible — but every non-GET outside `/auth/` is refused, games included. Suspending an admin is refused in the SQL predicate so the console cannot be locked.

Also shipped: a player-facing **Report** control on thought cards (`data-report-id`), without which the moderation queue could never receive anything. It sits in the same slot as Delete and is mutually exclusive with it. `POST /reports` takes the reporter from the verified session, never the body, and dedupes repeat filings through a partial unique index.

Console lives at `/admin/` (`js/admin-page/`: entry / state / actions / two renderers / shared helpers), seven tabs, hash-routed so a tab survives a reload. 512 API tests, 66 shared-frontend tests, 19 profile-social tests — all green.

## Repo-Wide Documentation Sync (2026-07-30)

A pass over every orientation doc in the repo to bring it back in line with the code, after a stretch of shipping (Play Store pipeline, badges, TA friends, ladders, payments, the domain move) that outran the docs. Claims were checked against the code and, where they were about tests, by running the suites.

Corrections worth remembering: the root `CLAUDE.md` grid-order list was wrong for **every** cabinet — the real order lives in each `games/<slug>/game.json` and had been reshuffled with Tactical Arena moved to 1, so the doc now says to re-read `game.json` rather than trusting a copied list. `AGENTS.md` told contributors to use **Jest**, which appears nowhere in the repo — everything is `node:test` or plain scripts with `node:assert`, and adding a framework is now explicitly ruled out. Cockpit Swarm's `game.json`, `CLAUDE.md`, and both READMEs disagreed about its campaign length; `STAGES` has **15** entries and `BOSS_EVERY = 5`, so the 15-stage/3-block README was right and the "5-stage" copy everywhere else was wrong (the menu subtitle interpolates `STAGES.length`, which is why nobody noticed in-game). `build-buddy` was simultaneously listed as a live grid cabinet and as "scoped in GDDs only". Last Bastion had grown from 3 stages/2 maps to 5 stages/4 battlefields.

Also marked shipped where docs still said pending: comment deletion (`COMMENT_DELETE_PLAN.md`, `BUGS.md`, the platform plan), password-reset sender verification, and the online login gate. `platform-api/README.md` was missing five of its thirteen route families and all seven catalog services.

## Test-Suite Repairs (2026-07-30)

Verifying the documentation pass above surfaced three broken or misleading test setups. All three are fixed; every cabinet suite in the repo is now green.

**Creature Battler was silently untested.** All eight of its test files are CommonJS, but there was no `package.json` under `games/creature-battle/`, so they inherited `"type": "module"` from the repo root and every one died at load with `ReferenceError: require is not defined in ES module scope` — nothing executed, under either `node <file>` or `node --test`. Adding `games/creature-battle/package.json` with `"type": "commonjs"` and a `test` script fixed the load. That is safe because the cabinet's own `scripts/*.js` are classic browser scripts with no `import`/`export`, and `js/mobile-controller.mjs` stays ESM on its extension.

Once they actually ran, four years-stale expectations surfaced — worth recording, because each one is a trap the next person will hit. The tests load game code by reading it with `fs` and evaluating it in a `vm` sandbox stubbed with hand-written collaborators, so **a new cross-file call in game code fails as a `ReferenceError`, not a meaningful assertion.** `battle-round.js` had grown calls to `accumulateBattleStats`, the end-of-round status/passive tickers, and `CreatureState.clearDefend`; `battle-engine.js` had grown the whole passive/skill-registry surface. Both needed new stubs. Sound-path expectations lacked the `creature-battler/` prefix that the outer platform entry point requires. And `SPD DOWN` labels are now stat-stage deltas (`SPD -1`) under the ±5-stage system.

The subtlest one: stubbing `getWarlordPresenceBonus` to `0` made **every** damage number collapse to `1`. Despite the name it is a multiplier (1.0 inactive, 1.08 active) folded into `calcDamage`'s `passiveMult`, so a zero silently floors damage to `ENGINE.MIN_DAMAGE` and presents as "the engine is broken" rather than "the stub is wrong". Stub multipliers as `1`.

**Battleshits' `npm test` ran eight of eleven suites.** The script chains files by name and omitted `audio`, `bot`, and `emojis` — all passing, all unguarded by the default command, including the entire bot AI. Added, along with focused `test:bot` / `test:audio` / `test:emojis` entries. The name-by-name style means a new test file still has to be registered by hand or it never runs.

**Mini-Tactics' failing main-menu test was the test's fault, not the product's.** `tests/main-menu.test.js` asserted a "Tutorials" button on the main menu; removing that button was a deliberate owner decision. The assertion was inverted to pin the button's *absence*, keeping the checks that the tutorial completion screen stays wired. `mainMenuScreen.js` already bound `startTutorial` optionally (`?.addEventListener`), so the menu works either way, and the tutorial subsystem remains intact behind the scenes. 143/143.

## Factory Launcher — Custom Domain + Progress-Sync Repair (2026-07-27)

**The site moved to `factory.jayarcade.com`.** The apex `jayarcade.com` belongs to the separate `loronajay/games-directory` repo (Jay's Retro Arcade and the Cabinet OS the Pi kiosks pull from) and must not be reassigned — GitHub allows one repo per domain. A subdomain avoids that entirely: one Namecheap CNAME (`factory` → `loronajay.github.io`), a `CNAME` file at the repo root, Enforce HTTPS on (Let's Encrypt cert issued 2026-07-28 01:18 UTC). The custom domain serves the repo at the **root**, not under `/javascript-game-factory/`; all in-site paths are relative so nothing broke, and GitHub redirects the old Pages URLs. `APP_BASE_URL` on Railway and the CORS allow-list in `platform-api/src/http-utils.mts` both moved with it (the old Pages origin stays allow-listed for old links). The privacy page's contact became `leojaylorona@gmail.com` — no mailbox exists on the domain.

Two operational gotchas found the hard way: saving the custom domain in Settings → Pages makes GitHub commit a `CNAME` straight to `main`, so the next local `git push` is rejected until you `git pull --rebase` (never force-push it — that unsets the domain); and Railway's GitHub connection had silently broken ("GitHub Repo not found"), so backend pushes were deploying nothing. Auto-deploy from `main` with root directory `platform-api` is reconnected.

**Progress sync: signed-in accounts could end up with progress the server never had.** The one-time local-ownership migration (`migration:local-ownership-v1`) could be *consumed without granting anything* — a fresh device (a new packaged-app install signing into an existing account) posted an empty backfill, which burned the one-shot for the whole account. The device that actually held the progress could then never migrate it, so the server owned nothing and every other surface correctly showed zero. Fixed in three places: an empty backfill is inert server-side, the client no longer posts one, and accounts already stranded self-heal while the server owns nothing for them. Verified end-to-end on a real device — units, skins, Valor, tutorials, and campaign clears all landed on the Android build from the server. Trust model and the follow-up to close the widened window: `games/tactical-arena/SECURITY.md`.

Debugging note worth keeping: Chromium batches localStorage writes into leveldb, so `adb`-pulled dumps of the packaged app are stale unless you `am force-stop` the app first. Several dumps during this investigation looked like the sync had never run when it had.

## Factory Launcher — Security Hardening (2026-07)

A hardening pass closed the money-critical holes. Backend (`platform-api/`): the public game-progress `/claims` route now rejects `premium-*` kinds (premium entitlements are Stripe-fulfillment-only); CORS is restricted to an allow-list (`https://loronajay.github.io` + localhost, extendable via `ALLOWED_ORIGINS`) instead of reflecting any origin with credentials; `/activity` requires auth and stamps the server-verified actor; JWT verification is pinned to HS256; per-IP rate limiting guards auth + Stripe checkout-session creation; uploads are validated by magic bytes (not the client MIME), which also guards the Cloudinary `raw` audio store. Tactical Arena's economy is now server-authoritative for signed-in players — Valor is spent through a server endpoint (atomic deduct+grant), and ownership is reconciled down to the server's set on boot (injected local ownership is dropped and self-heals) with a one-time backfill so no existing progress is lost. Full model + invariants in `games/tactical-arena/SECURITY.md`.

## Factory Launcher — Top Ladder Rankings (2026-07-25)

The "Top Ladder Rankings" panel on `/me` and `/player` had a normalize contract and a renderer but nothing behind it — both pages passed `ladderPlacements: []` forever. It now shows real cross-game standings.

The data was already there and already game-agnostic: `game_ratings (player_id, game_slug, rating, wins, losses, draws)` is written by `POST /ratings/:slug` (no slug allowlist) and by the ranked match reporter. What was missing was a platform-level read — the only leaderboard query lived inside the auth-gated `/ranked` family and folded in Tactical Arena's ranked cosmetics.

**The plug-in point is `platform-api/src/services/ladder-catalog.mts`.** Adding a game's ladder is one entry (`gameSlug`, `title`, `cabinetSlug`, `source`, `unitLabel`, `minMatches`) — the placement query, the public board route, `GET /ladders`, and both profile panels all read the registry, so which games rank is a data question, never a code question. The rating slug is deliberately separate from the cabinet slug: Sumorai stores ranked play under `sumorai-ranked` so casual matches never touch the ladder, and `title` lives in the registry because such a slug has no catalog entry to derive a name from. Registered today: `tactical-arena`, `sumorai-ranked`.

`db/ladders.mts` holds the two reads. `getPlayerLadderPlacements` ranks a player across every registered ladder in one query (window `rank()` per `game_slug`, joined against an `unnest` of the registry's slugs and per-ladder placement thresholds) and returns rank, field size, rating, and record. `getLadderStandings` is the public top-N board for one ladder. Both share one ordering — rating desc, win differential, then `player_id` — which makes the order total, so a board row and that player's profile placement can never disagree. Migration `025` adds the matching `game_ratings (game_slug, rating desc, …)` index; the primary key is `(player_id, game_slug)` and would have sorted the whole table per request.

`routes/ladder-routes.mts` serves `GET /ladders`, `GET /ladders/:gameSlug`, and `GET /players/:playerId/ladders`, all public — standings are public information, and the auth gate on `/ranked` exists because those routes also queue and report matches. The handler is dispatched **before** the `/players` family so the `ladders` suffix is not swallowed; a route-order test pins that, since a regression there would silently empty the rail rather than error.

Client-side, placements are fetched live (`fetchPlayerLadderPlacements`) by `me-page/page-data` and `player-page/loader`, never cached: a standing moves whenever anyone *else* on that ladder finishes a match, so a stored copy is stale by definition. `buildRankingItems` prefers live placements over whatever sits on the profile record and renders a fuller line — `1284 ELO` over `Rank #3 of 42 · 12W-4L-1D` — trimming to just `Rank #N` when a source carries nothing more. Empty states now say a placement has to be earned instead of promising standings are "coming online".

Still open: badges keyed off placement (`planning-docs/badge-plans/…` defers these pending ranked seasons and placement history), and a public board page — `GET /ladders/:slug` has no UI outside Tactical Arena's own in-game modal yet.

## Factory Launcher — Comment Moderation (2026-07-25)

Players can now delete comments on their own posts and photos, so moderation of a thread doesn't require deleting the whole post. The rule is the same on both surfaces: a comment can be removed by **its author** or by **the author of the post / owner of the photo**.

Backend owns the decision. `deleteThoughtComment` (`db/thoughts.mts`) and `deletePhotoComment` (`db/photos.mts`) load the comment and its parent inside a transaction, check the requester against both stored owner ids, delete, then recount and write back `comment_count`; they return `{ ok, reason }` so the routes can map `forbidden` → 403 and `not_found` → 404. `DELETE /thoughts/:id/comments/:commentId` and `DELETE /photos/:id/comments/:commentId` take the requester from the **verified session** (`authClaims.playerId`), never from the request body — `handleThoughtRoute` now receives `authClaims` for this reason (it previously took none).

**Every thought mutation is now account-holders-only, with the actor taken from the verified session.** `POST /thoughts`, `/thoughts/:id/comments`, `/shares`, and `/reactions` previously had no auth at all and read the actor from the request body — so any caller could post, comment, share, or react *as anyone*. All four now require a session and derive the actor from it; `POST /thoughts` also overwrites any body-supplied `authorPlayerId`. The photo comment/reaction routes already required a session but still read `actorPlayerId` from the body, letting any signed-in user act as another player; they now use the session id too.

The frontend had to follow, because `/thoughts` and `/player` are public-read pages whose social actions gated on the *local factory profile* — which every guest has. Both pages now resolve the auth session at boot and treat guests as read-only (`/me` already redirects to sign-in, so it was unaffected). Reading stayed public on purpose: opening a comment thread no longer requires a profile, and the sheet renders the thread for everyone but swaps the reply form for a "Sign in to join the conversation." prompt when signed out — the same shape the photo viewer already used for its comment form.

`DELETE /thoughts/:id` gained authorization in the same pass. It previously took an id and deleted, with no auth check at all — any caller who could reach the API could delete anyone's post. It now requires a verified session and only lets the post's author delete it. `deleteThought(db, thoughtId, requesterPlayerId)` carries the ownership check inside the delete statement (`where id = $1 and author_player_id = $2 returning id`) rather than doing a read-then-delete, so concurrent requests cannot race past it; when nothing is deleted it does a follow-up existence check purely to answer 404 vs 403. The response body is unchanged (`{ deleted, id }`), and the local-first client ignores the API result before deleting from its own cache, so guest posts (local-only, never persisted) still delete normally.

Deleting social content now also clears the notifications that announced it. Notifications embed a snapshot of what they reference, so a removed comment/post/photo used to leave a notification pointing at nothing. `deleteNotificationsByPayloadRef` (`db/notifications.mts`) deletes by `payload->>'<key>'`, with the key allowlisted to `commentId`/`thoughtId`/`photoId` since it is interpolated into a jsonb lookup; it swallows its own errors so cleanup can never fail the content deletion. Wired into four routes: comment delete (both surfaces) clears by `commentId`, post delete clears by `thoughtId`, photo delete clears by `photoId` — the last two only when the delete reports something was actually removed. No client change was needed: the notification badge and list refresh on page load and on each dropdown open, so a cleared notification simply stops appearing.

Frontend mirrors the rule only to decide whether the control is worth showing. `renderCommentSheet` reads `commentPanelState.viewerPlayerId` plus the new `commentTargetAuthorPlayerId` card field (for a repost, the *original* author — moderation follows the thread, not the card) and `buildCommentListHtml` reads `viewerPlayerId`/`isPhotoOwner`. The photo viewer gained `isPhotoOwner` alongside the existing `isOwner`: `isOwner` gates that surface's own "Remove Photo" button, `isPhotoOwner` is the identity fact, which is what the profile-embedded viewer needed (it offers comment moderation but not photo removal). Local-first `deleteThoughtCommentPost` mirrors the same rule for guests; `deleteThoughtCommentWithApi` only touches the local cache after the server confirms.

## Factory Launcher — Layout Editor UX Rebuild (2026-07-25)

`/me/layout` was rebuilt as a proper editor frame instead of a document page. The workspace is now full-bleed (it no longer inherits the shared 1120px `.arcade-shell` column) and locked to the viewport height as three independently-scrolling columns: panel rail, canvas viewport, inspector rail. The inspector moved out of the left sidebar — previously it sat *below* ~50 panel rows, so selecting anything pushed its own controls off-screen.

The left rail's flat dump of panels + composition elements became a grouped, searchable tree: `js/profile-layout/layout-panel-list.mts` (new module, extracted out of `layout-inspector-view.mts`) owns the pure grouping/filter model plus its markup. Rows group by profile section with a visible `enabled/total` count, groups auto-collapse when unused and auto-open on selection, labels wrap to two lines instead of truncating to `He…`, and each row carries an inline eye toggle so show/hide no longer needs a select → scroll → checkbox round-trip. The old per-row `HIDDEN`/`IDENTITY`/`ABOUT` badges — which were what squeezed the labels — are gone; state reads from row styling and the eye icon.

Canvas zoom was reworked around an explicit scroll stage (`buildZoomFrame` returns `stageWidth`/`stageHeight` instead of mutating the wrap's height/overflow), so zooming out shrinks the scroll area instead of leaving a tall empty gutter, and `MAX_ZOOM` was raised from `1` to `2` for fine composition-element work. Fit still never magnifies past 100%.

## Factory Launcher — Password Reset (2026-07-23)

Verified `RESEND_FROM_EMAIL` + custom domain now that a custom domain is live; swapped from the `onboarding@resend.dev` test sender (which only delivered to the Resend account owner's own inbox) so password reset works for all users. Sender is `JayArcade <noreply@jayarcade.com>`, domain verified in Resend via Namecheap DNS, `RESEND_API_KEY`/`RESEND_FROM_EMAIL`/`APP_BASE_URL` set on Railway, full forgot-password → email → reset flow confirmed working end-to-end in production.

## Factory Launcher — Recent Stabilization

- Friend rail avatar refresh now rehydrates stale existing friend slots instead of only appending missing previews.
- Pending friend-request conflicts now resolve to a friendly sent-state message instead of a generic retry error.
- Activity feed DB access now tolerates older deployed schemas missing newer columns, reducing `/activity` 500 risk during rollout mismatch.
- Major architecture cleanup: `js/player-page/`, `js/thoughts-page/`, `js/profile-editor/`, and `js/profile-social/` established as subsystems; `js/arcade-player.mjs`, `js/arcade-player-wire.mjs`, and `js/arcade-thoughts.mjs` reduced to compatibility shims; `thoughts-cards.mjs` extracted for card/view-model shaping.
- Backend route and contract cleanup: `platform-api/src/http-utils.mjs` and `platform-api/src/routes/` hold the extracted app seams; `relationships-domain.mjs`, `thoughts-domain.mjs`, and the `normalize-*` modules carry shared shaping logic that used to live in larger mixed files.
- Shared frontend renderer cleanup: `gallery-page/viewer.mjs` delegates to `viewer-state.mjs`, `viewer-social.mjs`, `viewer-page-actions.mjs`, and `viewer-page-controller.mjs`; `profile-social/social-view.mjs` delegates thought-card rendering to `social-view-thoughts.mjs` with shared helpers in `social-view-shared.mjs`.
- Shared signed-in nav centralized in `js/arcade-session-nav.mjs`; no longer hand-authored per page.
- CSS seam splits: `session-nav.css`, `profile-social.css`, `profile-page.css`, `profile-hero.css`, `profile-hero-card.css`, `profile-featured-cabinet.css`, `profile-identity.css`, and `profile-rail.css` extracted from the monolithic `me.css`/`player.css`.
- Shared page-shell CSS: `page-stage.css`, `grid-stage.css`, `profile-editor-card.css`, `gallery-viewer.css`, `thoughts-stage.css`, and `thoughts-feed.css` carry reusable framing that previously lived inside larger page-specific stylesheets.
- Percentage-based layout and mobile viewing experience pass complete across all platform pages.
- Browser tests moved out of root `js/` into `js/tests/` and subsystem `tests/` folders.
- Rounded UI pass: radius token system (`--profile-radius-xs/sm/md/lg/pill`) defined in `profile-hero.css` and applied across `me.css`, `profile-page.css`, `profile-hero-card.css`, `profile-identity.css`, `profile-featured-cabinet.css`, `profile-social.css`, `session-nav.css`, `profile-rail.css`, and `player.css`; centering pass applied to metrics stats, identity fields, about-me copy, badge empty text, and gallery upload button. Thoughts composer and feed action buttons still need the rounding pass (deferred).
- Profile CSS ownership cleanup: `profile-page.css` owns shared layout grid, column base, panel shell, and 980px responsive collapse for both `/me` and `/player`; `me.css` reduced to `/me`-specific theme vars/background/1480px reflow/friend navigator/panel ordering; `player.css` reduced to `/player`-specific theme vars/background/nav/gesture rail/challenge picker/friend actions.
- `/player` layout overhauled to match `/me`: column wrapper divs, independent-column stacking, right column panel order About → Badges → Feed.
- Primary nav rail made uniform across all non-home pages: Bulletins, Thoughts, and Activity added as nav items; `bulletins/index.html` and `grid.html` upgraded to the standard nav pattern.
- Auth no longer depends solely on cookies for cross-origin requests: `js/platform/api/auth-token.mjs` added as a localStorage token module; login/register responses include `token`; `auth-api.mjs` sends it as `Authorization: Bearer`; backend `extractTokenFromRequest` checks the Bearer header first then falls back to cookie — fixes login in Edge/Safari/third-party-cookie-blocked browsers.
- Profile editor extracted to its own standalone route `/me/edit/`; inline `<aside>` panel removed from `/me`; `js/profile-editor/editor-wire.mjs` is the entry point; `panel.mjs` works in both embedded and standalone modes; danger zone (Delete Account) moved from `/me` to `/me/edit/`.
- `/me/layout/` profile layout editor shipped: draggable panel system, 12-column grid, DB-backed save/load via `profile_layout` JSONB column (migration 017), `js/profile-layout/` subsystem, applied on `/me`/`/player` via a 3-column wrapper (`js/me-page/apply-layout.mjs`).
- Layout save/load bugs patched: `saveMyLayout`/`fetchMyLayout`/`fetchPlayerLayout` exposed as named client methods (previously internal, silently failing); `hasOverlap` check removed from `normalize-layout.mjs` (was discarding valid overlapping-after-drag layouts).
- Layout editor WYSIWYG + panel scaling overhaul (2026-05-16): editor canvas matches live profile grid width, drag/resize math fixed at zoom < 1, `MAX_ZOOM = 1`, hero card made fully draggable/resizable.
- Per-panel visual styling shipped (2026-05-16 to 2026-05-17): panel color/gradient/title-bubble/inner-element color, transparency, saturation, brightness, gradient angle — all via `--profile-panel-*` CSS custom properties, persisted inside the existing `profile_layout` JSONB payload.
- Real grid overlay in editor (2026-05-17): renders actual grid-cell elements on the same CSS Grid tracks as panels instead of a drifting background image.
- Live panel previews in editor (2026-05-18): `/me/layout` renders all current profile panels using live `/me` renderers and CSS.
- Hero sub-grid foundation shipped (2026-05-18): hero card supports registered `panel.children` entries for `portrait` and `metrics` in a 4×5 inner grid, draggable in the editor. Deeper child-level appearance controls still pending.
- Wire profile music: fully shipped — `profile_music_playlist` JSONB column (migration 015), Cloudinary `resource_type: raw` audio upload via `POST /upload/music`, 5-track structured playlist, drag-to-reorder editor, autoplay mini-player on `/me` and `/player`.

## Game-Level Feature History

Full narrative detail for games that now have their own `CLAUDE.md` — kept here for reference; the per-game file is the source of truth going forward.

- **mini-tactics**: turn-based isometric SVG squad-tactics. Deterministic headless core; hot-seat 2–4P (FFA + 2v2 teams), single-player vs CPU (3 difficulties), and online 2–4P (FFA + 2v2 teams) all shipped. Online is one unified lobby-based mode (1v1 = a 2-player lobby): deterministic lockstep over the server's generic v2 lobby relay, owner = state-hash authority, board/format/squads exchanged in-band, a dropped player is conceded by the remaining owner; the server change is just a config-only `lobbyGame` (limits 2–4) — no match engine. v1 code-complete + headless-validated (121/121) + real-server lobby smoke, not yet multi-tab hand-playtested. Full fantasy war-table visual identity, sound, Settings menu, custom squads, and a 3-tab How-to-Play reference are in.
- **tactical-arena**: turn-based isometric SVG tactics, the successor to mini-tactics. Keeps mini-tactics' activation rhythm and visuals but upgrades the engine (data-driven `arts` ability system, additive `statModifiers` seam, full status-effect lifecycle — blind/silence/poison/slow) and uses real per-unit stat blocks recovered from the original TurboWarp `.sb3`. Local hot-seat 1v1 duel, single-player vs CPU (Easy/Normal/Hard), and online 1v1 (a 2-player lobby). Roster is the full legacy drafted set (Swordsman, Archer, Mystic, Magician, Paladin, Necromancer, Sniper, summon-only Ghoul) plus a growing set of rebuild-original units (Witch Doctor, Father Time, Juggernaut, King, Monk, Angel, Gargoyle, Nemesis, Virus, Clod, the four-unit "fat squad", Miner, Riot Cop, Big Brother/Little Brother, Blacksword, Ronin, Mother Nature, Summoner, Treant). To-hit/crit rolls, damage forecast, body-block line-of-sight, Necromancer auras/summons, and a tile-object engine seam (Sniper's Build Cover walls + Throw Cigar fire + Rifle Powered passive) all in. Online Versus shipped: blind-pick, Quick Match + private code, deterministic lockstep, code-complete + headless-validated + real-server two-client relay smoke, not yet multi-tab hand-playtested. Board tokens use painted sprites with carved-SVG fallback. Also ships CPU, Skins, Nicknames, a 4-part Tutorial, a fifteen-mission story Campaign, and a standalone real-time Tempo Battle mode. 1037/1037 tests green (78 files).
- **illuminauts**: 2-player online maze race with suit-light fog, relay-based multiplayer via Factory Network; Phase 4 online code complete with reworked lobby (side-select, multi-phase, hover effects, connect-once pattern matching Lovers Lost); walking animation shipped (3-frame per-direction sprites, idle reset, beta palette recolored); playtested and working; current bottleneck is map count, Phase 5 procedural generation is next.
- **circuit-siege**: fully playable online 1v1 route-repair puzzle. Server-authoritative match engine, compact map format (blue-only authored, red x-mirrored at load), public + private matchmaking, 5-minute timer, damage/dud route distinction, result screen, internal map editor. Phase 4a UX polish complete (slot pulse animations, score pips, held-piece text); Phase 4b planned (Play Again button, number-key tool selection, Escape to drop).
- **echo-duel**: 2-6 player online memory duel with lobby-based play and server-authoritative state; also ships a solo survival mode (score-attack: how many 10-input chains before you spell the penalty word). Major UI pass complete (2026-05-19): match screen uses full-bleed HUD frame, WASD input chips with lit/dim states + per-key glow, mini chips in sequence slots, waveform atmosphere layer, phase flash overlay. Single-player results screen reads as survival mode with personal best tracked in `echo-duel.pb.solo`.
- **battleshits**: fully playable Solo Battle (Classic Bot Battle) mode — Easy/Medium/Hard difficulty, bot fleet placement, full turn loop, dual-side match stats on result screen; implemented via a local net facade (`scripts/bot-battle.js`) that leaves `battle.js` unchanged.
- **sumorai**: fully shipped one-hit-kill samurai fighter ported from TurboWarp: local 2P, vs CPU (Easy/Medium/Hard), control remapping, and online play (lobby, matchmaking, private rooms, Best of 5). Online uses GGPO-style rollback netcode (12-frame window) with a ping/rollback network-health HUD; ranked mode on a separate `sumorai-ranked` queue backed by the platform ELO system; match results publish to the activity feed; no round timer by design.
- **creature-battler**: 12 creatures, a 7-tier level system with class/skill/passive customization, full training mode (solo vs AI), and a working Online 1v1 blind-pick mode with synced RNG, disconnect handling, and private rooms. Timeline-based animation engine complete with all 12 creatures' full move sets (15 moves each). Draft pick scoped (roster threshold of 12 met) but not yet implemented.
- **Lovers Lost**: solo mode has per-side personal best tracking (high score any run, best time completed runs only) persisted in localStorage via `lovers-lost.pb.boy` / `lovers-lost.pb.girl`.

## Platform Architecture Cleanup History

- Backend Cutover: all 6 passes shipped. Authenticated users now read and write through the API only; localStorage is a write-through cache or guest-only. Merge helpers deleted. Full history in `planning-docs/CUTOVER_HANDOFF.md`.
- Shared Platform architecture pass complete: `platform-api/src/app.mjs` is mostly a dispatch shell, route families live in `src/routes/`, `normalize.mjs` is a thin barrel, `js/me-page/` subsystem is complete (all `/me` logic extracted from root `arcade-me-*` files; `arcade-me.mjs` is now a 4-line shim), gallery viewer behavior is split into dedicated modules, and `profile-social/social-view.mjs` no longer owns the nested thought-card renderer by itself.
- Folder reorg complete: all page-subsystem folders established — `js/me-page/`, `js/player-page/`, `js/thoughts-page/`, `js/gallery-page/`, `js/profile-editor/`, `js/profile-social/`.
- TypeScript migration: the entire non-game migration (Phases 0–9) completed 2026-05-30 — all of `js/**` and `platform-api/src/**` are `.mts` under `strict: true`, with emitted `.mjs` committed in-place (browser) / synced beside source (backend); `typecheck:browser` + `typecheck:api` clean, test baselines green (91/91 browser, 116/116 backend). Phase 8 converted the last 3 classic global scripts (`pixel-text`/`arcade-input`/`platform-config`) to typed ES modules that still attach their globals; `js/globals.d.ts` is the permanent ambient contract. Only the game cabinets remain, migrated last per-cabinet after each one's own seam cleanup. Plan: `planning-docs/TYPESCRIPT_MIGRATION_PLAN.md`.
