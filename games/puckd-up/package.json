{
  "name": "puckd-up",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:network": "node server/sync-server.mjs --check && node --test tests/network-contract.mjs",
    "sync:server": "node server/sync-server.mjs"
  }
}
